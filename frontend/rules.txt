<USER_REQUEST>
Here is the full prompt — plain text, ready to paste:

You are a senior frontend engineer. Your task is to make the entire admin dashboard application fully responsive for mobile (320px–767px) and tablet (768px–1023px) screens. The landing page is already mobile-optimized. The DashboardLayout.tsx shell is already mobile-optimized. Do NOT touch those. Your job is to fix every inner page, subpage, dialog box, popup, dropdown, modal, and filter bar inside src/pages/ and src/components/.

Do NOT read README files or documentation. Open and edit the actual source files directly. Quote the before and after code for every change you make.

RULE 1 — Global Flex Row Fix
Search the entire codebase for every instance of:

tsx
className="flex items-center gap-*"
For every one of these that contains multiple buttons, inputs, dropdowns, or interactive elements side by side — add flex-wrap to it. If the row contains a primary action area and a secondary toggle/switch, convert it to:

tsx
// BEFORE
<truncated 11199 bytes>