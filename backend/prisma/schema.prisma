generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["fullTextIndex", "fullTextSearch"]
  binaryTargets   = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Institution {
  id                             String                          @id @default(uuid()) @db.Char(36)
  name                           String                          @db.VarChar(255)
  code                           String                          @unique @db.VarChar(50)
  institutionType                InstitutionType                 @default(SCHOOL) @map("institution_type")
  address                        String?                         @db.Text
  contactEmail                   String?                         @map("contact_email") @db.VarChar(255)
  contactPhone                   String?                         @map("contact_phone") @db.VarChar(20)
  academicYear                   String                          @default("2025-2026") @map("academic_year") @db.VarChar(20)
  logoUrl                        String?                         @map("logo_url") @db.VarChar(500)
  darkLogoUrl                    String?                         @map("dark_logo_url") @db.VarChar(500)
  signatureUrl                   String?                         @map("signature_url") @db.VarChar(500)
  signatureTitle                 String                          @default("Principal") @map("signature_title") @db.VarChar(100)
  sealUrl                        String?                         @map("seal_url") @db.VarChar(500)
  enabledFields                  Json                            @map("enabled_fields")
  customFields                   Json                            @map("custom_fields")
  enabledServices                Json                            @map("enabled_services")
  subscriptionTier               SubscriptionTier                @default(starter) @map("subscription_tier")
  subscriptionStatus             SubscriptionStatus              @default(trial) @map("subscription_status")
  trialEndsAt                    DateTime?                       @map("trial_ends_at")
  subscriptionStart              DateTime?                       @map("subscription_start")
  subscriptionEnd                DateTime?                       @map("subscription_end")
  monthlyAiUsage                 Int                             @default(0) @map("monthly_ai_usage")
  monthlyPdfPages                Int                             @default(0) @map("monthly_pdf_pages")
  monthlyEmailSent               Int                             @default(0) @map("monthly_email_sent")
  storageUsedMb                  Decimal                         @default(0.00) @map("storage_used_mb") @db.Decimal(10, 2)
  isActive                       Boolean                         @default(true) @map("is_active")
  onboardingCompleted            Boolean                         @default(false) @map("onboarding_completed")
  createdAt                      DateTime                        @default(now()) @map("created_at")
  updatedAt                      DateTime                        @updatedAt @map("updated_at")
  whatsappPhoneNumberId          String?                         @map("whatsapp_phone_number_id") @db.VarChar(50)
  whatsappWabaId                 String?                         @map("whatsapp_waba_id") @db.VarChar(50)
  razorpayAccountId              String?                         @map("razorpay_account_id") @db.VarChar(100)
  monthlyWhatsappSent            Int                             @default(0) @map("monthly_whatsapp_sent")
  featureOverrides               Json?                           @map("feature_overrides")
  moduleConfig                   Json?                           @map("module_config")
  adminInvitations               AdminInvitation[]
  alumni                         Alumni[]
  alumniEvents                   AlumniEvent[]
  approvalRequests               ApprovalRequest[]
  approvalWorkflows              ApprovalWorkflow[]
  approvals                      Approval[]
  assignmentSubmissions          AssignmentSubmission[]
  assignments                    Assignment[]
  attendanceLogs                 AttendanceLog[]
  attendanceSessions             AttendanceSession[]
  auditLogs                      AuditLog[]
  biometricDevices               BiometricDevice[]
  biometricPunches               BiometricPunch[]
  branches                       Branch[]
  calculationEngines             CalculationEngine[]
  calendarEvents                 CalendarEvent[]
  cceAssessments                 CceAssessment[]
  cceMarks                       CceMark[]
  certificates                   Certificate[]
  classes                        Class[]
  clinicVisits                   ClinicVisit[]
  conversationMessages           ConversationMessage[]
  conversations                  Conversation[]
  digestQueue                    DigestQueue[]
  enquiries                      Enquiry[]
  enquiryActivities              EnquiryActivity[]
  examSchedules                  ExamSchedule[]
  feeConcessions                 FeeConcession[]
  feeInstallmentPlans            FeeInstallmentPlan[]
  feeInstallments                FeeInstallment[]
  feeInvoices                    FeeInvoice[]
  feePaymentClaims               FeePaymentClaim[]
  feePayments                    FeePayment[]
  feeStructures                  FeeStructure[]
  gatePasses                     GatePass[]
  groupPhotos                    GroupPhoto[]
  guardianStudentLinks           GuardianStudentLink[]
  guardians                      Guardian[]
  hallTickets                    HallTicket[]
  healthRecords                  HealthRecord[]
  hostelAllotments               HostelAllotment[]
  hostelBlocks                   HostelBlock[]
  hostelRooms                    HostelRoom[]
  idCardBatches                  IdCardBatch[]
  idCards                        IdCard[]
  inboundMedia                   InboundMedia[]
  authorities                    InstitutionAuthority[]
  inventoryCategories            InventoryCategory[]
  inventoryItems                 InventoryItem[]
  jobExecutions                  JobExecution[]
  journalEntries                 JournalEntry[]
  ledgerAccounts                 LedgerAccount[]
  libraryCards                   LibraryCard[]
  liveClasses                    LiveClass[]
  marksheets                     Marksheet[]
  messBills                      MessBill[]
  messageTemplates               MessageTemplate[]
  messages                       Message[]
  notices                        Notice[]
  notificationLogs               NotificationLog[]
  notificationTemplates          NotificationTemplate[]
  notifications                  Notification[]
  onlineTestAttempts             OnlineTestAttempt[]
  onlineTests                    OnlineTest[]
  outbox                         Outbox[]
  placementApplications          PlacementApplication[]
  placementDrives                PlacementDrive[]
  portfolios                     Portfolio[]
  questionBankItems              QuestionBankItem[]
  savedReports                   SavedReport[]
  sections                       Section[]
  socialPosts                    SocialPost[]
  socialRelationships            SocialRelationship[]
  staffAttendances               StaffAttendance[]
  staffMembers                   StaffMember[]
  stockTransactions              StockTransaction[]
  streams                        Stream[]
  StudentFormProgress            StudentFormProgress[]
  StudentOnboardingToken         StudentOnboardingToken[]
  students                       Student[]
  subjects                       Subject[]
  templates                      Template[]
  timetablePeriods               TimetablePeriod[]
  transferCertificates           TransferCertificate[]
  transportRoutes                TransportRoute[]
  userRoles                      UserInstitutionRole[]
  vaccinationRecords             VaccinationRecord[]
  visionariumArticles            VisionariumArticle[]
  visionariumSchoolSubscriptions VisionariumSchoolSubscription[]
  visionariumSubmissions         VisionariumSubmission[]
  visionariumTestSeries          VisionariumTestSeries[]
  visitingCards                  VisitingCard[]
  visitorLogs                    VisitorLog[]

  @@index([code])
  @@index([subscriptionStatus, subscriptionTier])
  @@index([isActive])
  @@map("institutions")
}

model ChatConversation {
  id            String            @id @default(uuid()) @db.Char(36)
  institutionId String            @map("institution_id") @db.Char(36)
  type          ConversationType  @default(direct)
  name          String?           @db.VarChar(255)
  lastMessageAt DateTime?         @map("last_message_at")
  createdAt     DateTime          @default(now()) @map("created_at")
  updatedAt     DateTime          @updatedAt @map("updated_at")
  messages      ChatMessage[]
  participants  ChatParticipant[]

  @@map("chat_conversations")
}

model ChatParticipant {
  id             String           @id @default(uuid()) @db.Char(36)
  conversationId String           @map("conversation_id") @db.Char(36)
  userId         String           @map("user_id") @db.Char(36)
  isAdmin        Boolean          @default(false) @map("is_admin")
  joinedAt       DateTime         @default(now()) @map("joined_at")
  lastReadAt     DateTime?        @map("last_read_at")
  conversation   ChatConversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  user           User             @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([conversationId, userId])
  @@index([userId], map: "chat_participants_user_id_fkey")
  @@map("chat_participants")
}

model ChatMessage {
  id             String           @id @default(uuid()) @db.Char(36)
  conversationId String           @map("conversation_id") @db.Char(36)
  senderId       String           @map("sender_id") @db.Char(36)
  type           MessageType      @default(text)
  content        String           @db.Text
  metadata       Json?
  status         MessageStatus    @default(sent)
  createdAt      DateTime         @default(now()) @map("created_at")
  updatedAt      DateTime         @updatedAt @map("updated_at")
  conversation   ChatConversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  sender         User             @relation("SentMessages", fields: [senderId], references: [id], onDelete: Cascade)

  @@index([conversationId], map: "chat_messages_conversation_id_fkey")
  @@index([senderId], map: "chat_messages_sender_id_fkey")
  @@map("chat_messages")
}

model CallSession {
  id              String    @id @default(uuid()) @db.Char(36)
  institutionId   String    @map("institution_id") @db.Char(36)
  conversationId  String?   @map("conversation_id") @db.Char(36)
  livekitRoomName String    @map("livekit_room_name") @db.VarChar(255)
  type            String    @default("video") @db.VarChar(20)
  hostId          String    @map("host_id") @db.Char(36)
  startedAt       DateTime  @default(now()) @map("started_at")
  endedAt         DateTime? @map("ended_at")
  participants    Json
  host            User      @relation("HostedCalls", fields: [hostId], references: [id])

  @@index([hostId], map: "call_sessions_host_id_fkey")
  @@map("call_sessions")
}

model InstitutionAuthority {
  id            String                   @id @default(uuid()) @db.Char(36)
  institutionId String                   @map("institution_id") @db.Char(36)
  name          String                   @db.VarChar(255)
  designation   String                   @db.VarChar(255)
  roleType      InstitutionAuthorityRole @default(CUSTOM) @map("role_type")
  email         String?                  @db.VarChar(255)
  phone         String?                  @db.VarChar(20)
  signatureUrl  String?                  @map("signature_url") @db.VarChar(500)
  displayOrder  Int                      @default(0) @map("display_order")
  createdAt     DateTime                 @default(now()) @map("created_at")
  updatedAt     DateTime                 @updatedAt @map("updated_at")
  institution   Institution              @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([roleType])
  @@map("institution_authorities")
}

model Branch {
  id            String      @id @default(uuid()) @db.Char(36)
  institutionId String      @map("institution_id") @db.Char(36)
  name          String      @db.VarChar(255)
  code          String      @db.VarChar(50)
  address       String?     @db.Text
  contactEmail  String?     @map("contact_email") @db.VarChar(255)
  contactPhone  String?     @map("contact_phone") @db.VarChar(20)
  isActive      Boolean     @default(true) @map("is_active")
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")
  institution   Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  classes       Class[]
  students      Student[]

  @@unique([institutionId, code])
  @@index([institutionId])
  @@map("branches")
}

model User {
  id                        String                  @id @default(uuid()) @db.Char(36)
  email                     String                  @unique @db.VarChar(255)
  passwordHash              String?                 @map("password_hash") @db.VarChar(255)
  name                      String                  @db.VarChar(255)
  phone                     String?                 @db.VarChar(20)
  emailVerified             Boolean                 @default(false) @map("email_verified")
  image                     String?                 @db.Text
  isActive                  Boolean                 @default(true) @map("is_active")
  isVerified                Boolean                 @default(false) @map("is_verified")
  emailVerificationToken    String?                 @map("email_verification_token") @db.VarChar(255)
  passwordResetToken        String?                 @map("password_reset_token") @db.VarChar(255)
  passwordResetExpires      DateTime?               @map("password_reset_expires")
  twoFactorEnabled          Boolean                 @default(false) @map("two_factor_enabled")
  twoFactorSecret           String?                 @map("two_factor_secret") @db.VarChar(255)
  globalRole                GlobalRole?             @default(student) @map("global_role")
  lastLoginAt               DateTime?               @map("last_login_at")
  lastLoginIp               String?                 @map("last_login_ip") @db.VarChar(50)
  failedLoginAttempts       Int                     @default(0) @map("failed_login_attempts")
  lockedUntil               DateTime?               @map("locked_until")
  createdAt                 DateTime                @default(now()) @map("created_at")
  updatedAt                 DateTime                @updatedAt @map("updated_at")
  alternateEmails           Json?                   @map("alternate_emails")
  externalSubjects          Json?                   @map("external_subjects")
  accounts                  Account[]
  approvalRequests          ApprovalRequest[]
  approvedSteps             ApprovalStep[]
  reviewedApprovals         Approval[]              @relation("ReviewedApprovals")
  submittedApprovals        Approval[]              @relation("SubmittedApprovals")
  markedAttendanceRecords   AttendanceRecord[]
  createdAttendanceSessions AttendanceSession[]
  auditLogs                 AuditLog[]
  hostedCalls               CallSession[]           @relation("HostedCalls")
  sentMessages              ChatMessage[]           @relation("SentMessages")
  chatParticipations        ChatParticipant[]
  notifications             Notification[]
  oauthAccessTokens         OauthAccessToken[]
  oauthApplications         OauthApplication[]
  oauthConsents             OauthConsent[]
  classTeacherSections      Section[]               @relation("SectionClassTeacher")
  sessions                  Session[]
  socialComments            SocialComment[]         @relation("UserComments")
  socialPosts               SocialPost[]            @relation("UserPosts")
  socialReactions           SocialReaction[]        @relation("UserReactions")
  relationshipsFrom         SocialRelationship[]    @relation("RelationshipsFrom")
  relationshipsTo           SocialRelationship[]    @relation("RelationshipsTo")
  saathiSent                SocialSaathiLink[]      @relation("SaathiSent")
  saathiReceived            SocialSaathiLink[]      @relation("SaathiReceived")
  student                   Student?
  institutionRoles          UserInstitutionRole[]
  authoredArticles          VisionariumArticle[]    @relation("UserArticles")
  visionariumSubmissions    VisionariumSubmission[] @relation("UserSubmissions")
  visitingCards             VisitingCard[]

  @@index([email])
  @@index([passwordResetToken])
  @@map("users")
}

model AdminInvitation {
  id            String           @id @default(uuid()) @db.Char(36)
  institutionId String           @map("institution_id") @db.Char(36)
  email         String           @db.VarChar(255)
  token         String           @unique @db.VarChar(255)
  expiresAt     DateTime         @map("expires_at")
  status        InvitationStatus @default(pending)
  createdAt     DateTime         @default(now()) @map("created_at")
  updatedAt     DateTime         @updatedAt @map("updated_at")
  institution   Institution      @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([email])
  @@index([token])
  @@index([institutionId])
  @@map("admin_invitations")
}

model UserInstitutionRole {
  id                   String          @id @default(uuid()) @db.Char(36)
  userId               String          @map("user_id") @db.Char(36)
  institutionId        String          @map("institution_id") @db.Char(36)
  role                 InstitutionRole
  assignedClasses      Json?           @map("assigned_classes")
  assignedSections     Json?           @map("assigned_sections")
  studentAccessEnabled Boolean         @default(false) @map("student_access_enabled")
  studentAccessExpires DateTime?       @map("student_access_expires")
  createdAt            DateTime        @default(now()) @map("created_at")
  updatedAt            DateTime        @updatedAt @map("updated_at")
  institution          Institution     @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  user                 User            @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, institutionId])
  @@index([role])
  @@index([institutionId])
  @@map("user_institution_roles")
}

model Class {
  id                    String                  @id @default(uuid()) @db.Char(36)
  institutionId         String                  @map("institution_id") @db.Char(36)
  branchId              String?                 @map("branch_id") @db.Char(36)
  name                  String                  @db.VarChar(100)
  displayOrder          Int                     @default(0) @map("display_order")
  streamsEnabled        Boolean                 @default(false) @map("streams_enabled")
  createdAt             DateTime                @default(now()) @map("created_at")
  updatedAt             DateTime                @updatedAt @map("updated_at")
  attendanceSessions    AttendanceSession[]
  branch                Branch?                 @relation(fields: [branchId], references: [id])
  institution           Institution             @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  groupPhotos           GroupPhoto[]
  sections              Section[]
  socialPosts           SocialPost[]
  streams               Stream[]
  subjects              Subject[]
  visionariumTestSeries VisionariumTestSeries[]

  @@unique([institutionId, name])
  @@index([institutionId])
  @@index([branchId])
  @@map("classes")
}

model Stream {
  id            String      @id @default(uuid()) @db.Char(36)
  institutionId String      @map("institution_id") @db.Char(36)
  classId       String      @map("class_id") @db.Char(36)
  name          String      @db.VarChar(100)
  description   String?     @db.Text
  displayOrder  Int         @default(0) @map("display_order")
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")
  sections      Section[]
  class         Class       @relation(fields: [classId], references: [id], onDelete: Cascade)
  institution   Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([classId, name])
  @@index([institutionId], map: "streams_institution_id_fkey")
  @@map("streams")
}

model Section {
  id                   String              @id @default(uuid()) @db.Char(36)
  institutionId        String              @map("institution_id") @db.Char(36)
  classId              String              @map("class_id") @db.Char(36)
  streamId             String?             @map("stream_id") @db.Char(36)
  name                 String              @db.VarChar(50)
  expectedStudentCount Int                 @default(0) @map("expected_student_count")
  createdAt            DateTime            @default(now()) @map("created_at")
  updatedAt            DateTime            @updatedAt @map("updated_at")
  classTeacherId       String?             @map("class_teacher_id") @db.Char(36)
  admissionSlots       AdmissionSlot[]
  attendanceSessions   AttendanceSession[]
  groupPhotos          GroupPhoto[]
  class                Class               @relation(fields: [classId], references: [id], onDelete: Cascade)
  classTeacher         User?               @relation("SectionClassTeacher", fields: [classTeacherId], references: [id])
  institution          Institution         @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  stream               Stream?             @relation(fields: [streamId], references: [id])
  socialPosts          SocialPost[]
  students             Student[]

  @@unique([classId, streamId, name])
  @@index([classTeacherId], map: "sections_class_teacher_id_fkey")
  @@index([institutionId], map: "sections_institution_id_fkey")
  @@index([streamId], map: "sections_stream_id_fkey")
  @@map("sections")
}

model AdmissionSlot {
  id             String     @id @default(cuid()) @db.Char(30)
  sectionId      String     @map("section_id") @db.Char(36)
  rollNo         Int        @map("roll_no")
  status         SlotStatus @default(EMPTY)
  token          String     @unique @default(cuid()) @db.VarChar(50)
  tokenExpiresAt DateTime?  @map("token_expires_at")
  createdAt      DateTime   @default(now()) @map("created_at")
  section        Section    @relation(fields: [sectionId], references: [id], onDelete: Cascade)
  student        Student?

  @@unique([sectionId, rollNo])
  @@index([sectionId])
  @@index([status])
  @@index([token])
  @@map("admission_slots")
}

model Student {
  id                      String                   @id @default(uuid()) @db.Char(36)
  institutionId           String                   @map("institution_id") @db.Char(36)
  sectionId               String                   @map("section_id") @db.Char(36)
  branchId                String?                  @map("branch_id") @db.Char(36)
  slotId                  String?                  @unique @map("slot_id") @db.Char(30)
  admissionNumber         String?                  @unique @map("admission_number") @db.VarChar(50)
  name                    String                   @db.VarChar(255)
  academicYear            String                   @default("2025-2026") @map("academic_year") @db.VarChar(20)
  fatherName              String?                  @map("father_name") @db.VarChar(255)
  motherName              String?                  @map("mother_name") @db.VarChar(255)
  guardianName            String?                  @map("guardian_name") @db.VarChar(255)
  guardianRelation        String?                  @map("guardian_relation") @db.VarChar(50)
  guardianPhone           String?                  @map("guardian_phone") @db.VarChar(20)
  sex                     Sex?
  dob                     DateTime?                @db.Date
  bloodGroup              String?                  @map("blood_group") @db.VarChar(10)
  aadharNumber            String?                  @map("aadhar_number") @db.VarChar(12)
  caste                   String?                  @db.VarChar(100)
  religion                String?                  @db.VarChar(100)
  contact                 String?                  @db.VarChar(20)
  parentEmail             String?                  @map("parent_email") @db.VarChar(255)
  address                 String?                  @db.Text
  city                    String?                  @db.VarChar(100)
  state                   String?                  @db.VarChar(100)
  pincode                 String?                  @db.VarChar(10)
  dateOfAdmission         DateTime?                @map("date_of_admission") @db.Date
  previousSchool          String?                  @map("previous_school") @db.Text
  transportMode           String?                  @map("transport_mode") @db.VarChar(50)
  photoUrl                String?                  @map("photo_url") @db.VarChar(500)
  photoHash               String?                  @map("photo_hash") @db.VarChar(64)
  studentSignatureUrl     String?                  @map("student_signature_url") @db.VarChar(500)
  parentSignatureUrl      String?                  @map("parent_signature_url") @db.VarChar(500)
  medicalNotes            String?                  @map("medical_notes") @db.Text
  status                  StudentStatus            @default(active)
  dataStatus              DataStatus               @default(filled) @map("data_status")
  consentGiven            Boolean                  @default(false) @map("consent_given")
  consentTimestamp        DateTime?                @map("consent_timestamp")
  consentGivenBy          String?                  @map("consent_given_by") @db.VarChar(255)
  customData              Json                     @map("custom_data")
  createdAt               DateTime                 @default(now()) @map("created_at")
  updatedAt               DateTime                 @updatedAt @map("updated_at")
  photoMetadata           Json?                    @map("photo_metadata")
  photoUpdatedAt          DateTime?                @map("photo_updated_at")
  photoVersion            Int                      @default(0) @map("photo_version")
  thumbUrl                String?                  @map("thumb_url") @db.VarChar(500)
  userId                  String?                  @unique @map("user_id") @db.Char(36)
  rollNumber              String?                  @map("roll_number") @db.VarChar(50)
  attendanceLogs          AttendanceLog[]
  attendanceRecords       AttendanceRecord[]
  certificates            Certificate[]
  feeInvoices             FeeInvoice[]
  groupPhotoExtractions   GroupPhotoExtraction[]
  guardianLinks           GuardianStudentLink[]
  hallTickets             HallTicket[]
  idCards                 IdCard[]
  libraryCards            LibraryCard[]
  marks                   Mark[]
  marksheets              Marksheet[]
  portfolio               Portfolio?
  socialPosts             SocialPost[]             @relation("StudentPosts")
  formProgress            StudentFormProgress?
  onboardingToken         StudentOnboardingToken?
  branch                  Branch?                  @relation(fields: [branchId], references: [id])
  institution             Institution              @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  section                 Section                  @relation(fields: [sectionId], references: [id], onDelete: Cascade)
  admissionSlot           AdmissionSlot?           @relation(fields: [slotId], references: [id])
  user                    User?                    @relation(fields: [userId], references: [id])
  transferCertificates    TransferCertificate[]
  authoredArticles        VisionariumArticle[]     @relation("StudentArticles")
  visionariumSubmissions  VisionariumSubmission[]  @relation("StudentSubmissions")
  visionariumTestAttempts VisionariumTestAttempt[] @relation("StudentTestAttempts")
  visitingCards           VisitingCard[]

  @@index([institutionId])
  @@index([branchId])
  @@index([sectionId])
  @@index([slotId])
  @@index([status])
  @@index([dataStatus])
  @@index([photoHash])
  @@index([institutionId, status])
  @@index([institutionId, dataStatus])
  @@fulltext([name, fatherName, motherName, admissionNumber])
  @@map("students")
}

model Template {
  id                   String                @id @default(uuid()) @db.Char(36)
  institutionId        String                @map("institution_id") @db.Char(36)
  name                 String                @db.VarChar(255)
  serviceType          ServiceType           @map("service_type")
  templateType         TemplateType          @default(html) @map("template_type")
  content              String                @db.LongText
  cssStyles            String?               @map("css_styles") @db.Text
  widthMm              Decimal               @default(85.60) @map("width_mm") @db.Decimal(7, 2)
  heightMm             Decimal               @default(54.00) @map("height_mm") @db.Decimal(7, 2)
  orientation          Orientation           @default(landscape)
  thumbnailUrl         String?               @map("thumbnail_url") @db.VarChar(500)
  description          String?               @db.Text
  isDefault            Boolean               @default(false) @map("is_default")
  isActive             Boolean               @default(true) @map("is_active")
  targetAudience       TemplateAudience      @default(ALL) @map("target_audience")
  createdAt            DateTime              @default(now()) @map("created_at")
  updatedAt            DateTime              @updatedAt @map("updated_at")
  certificates         Certificate[]
  hallTickets          HallTicket[]
  idCardBatches        IdCardBatch[]
  idCards              IdCard[]
  libraryCards         LibraryCard[]
  marksheets           Marksheet[]
  portfolios           Portfolio[]
  institution          Institution           @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  transferCertificates TransferCertificate[]
  visitingCards        VisitingCard[]

  @@index([institutionId])
  @@index([serviceType])
  @@index([isDefault])
  @@index([targetAudience])
  @@index([institutionId, serviceType])
  @@map("templates")
}

model IdCard {
  id                  String       @id @default(uuid()) @db.Char(36)
  studentId           String       @map("student_id") @db.Char(36)
  institutionId       String       @map("institution_id") @db.Char(36)
  templateId          String?      @map("template_id") @db.Char(36)
  cardNumber          String?      @map("card_number") @db.VarChar(50)
  validFrom           DateTime?    @map("valid_from") @db.Date
  validUntil          DateTime?    @map("valid_until") @db.Date
  qrCodeData          String?      @map("qr_code_data") @db.Text
  barcodeData         String?      @map("barcode_data") @db.VarChar(100)
  cardFrontUrl        String?      @map("card_front_url") @db.VarChar(500)
  cardBackUrl         String?      @map("card_back_url") @db.VarChar(500)
  pdfUrl              String?      @map("pdf_url") @db.VarChar(500)
  status              IdCardStatus @default(draft)
  issuedAt            DateTime?    @map("issued_at")
  createdAt           DateTime     @default(now()) @map("created_at")
  updatedAt           DateTime     @updatedAt @map("updated_at")
  pdfObjectPath       String?      @map("pdf_object_path") @db.VarChar(500)
  cardBackObjectPath  String?      @map("card_back_object_path") @db.VarChar(500)
  cardFrontObjectPath String?      @map("card_front_object_path") @db.VarChar(500)
  institution         Institution  @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student             Student      @relation(fields: [studentId], references: [id], onDelete: Cascade)
  template            Template?    @relation(fields: [templateId], references: [id])

  @@unique([institutionId, cardNumber])
  @@index([studentId])
  @@index([status])
  @@index([institutionId])
  @@index([institutionId, status])
  @@index([templateId])
  @@map("id_cards")
}

model VisitingCard {
  id            String             @id @default(uuid()) @db.Char(36)
  studentId     String?            @map("student_id") @db.Char(36)
  userId        String?            @map("user_id") @db.Char(36)
  institutionId String             @map("institution_id") @db.Char(36)
  templateId    String?            @map("template_id") @db.Char(36)
  cardNumber    String             @map("card_number") @db.VarChar(50)
  designation   String?            @db.VarChar(255)
  department    String?            @db.VarChar(255)
  email         String?            @db.VarChar(255)
  phone         String?            @db.VarChar(20)
  website       String?            @db.VarChar(255)
  linkedinUrl   String?            @map("linkedin_url") @db.VarChar(500)
  frontPdfUrl   String?            @map("front_pdf_url") @db.VarChar(500)
  backPdfUrl    String?            @map("back_pdf_url") @db.VarChar(500)
  thumbnailUrl  String?            @map("thumbnail_url") @db.VarChar(500)
  qrCodeData    String?            @map("qr_code_data") @db.Text
  status        VisitingCardStatus @default(draft)
  issuedAt      DateTime?          @map("issued_at")
  createdAt     DateTime           @default(now()) @map("created_at")
  updatedAt     DateTime           @updatedAt @map("updated_at")
  institution   Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student       Student?           @relation(fields: [studentId], references: [id])
  template      Template?          @relation(fields: [templateId], references: [id])
  user          User?              @relation(fields: [userId], references: [id])

  @@unique([institutionId, cardNumber])
  @@index([institutionId])
  @@index([status])
  @@index([userId])
  @@index([studentId])
  @@index([templateId], map: "visiting_cards_template_id_fkey")
  @@map("visiting_cards")
}

model GroupPhoto {
  id                            String                     @id @default(uuid()) @db.Char(36)
  institutionId                 String                     @map("institution_id") @db.Char(36)
  sectionId                     String?                    @map("section_id") @db.Char(36)
  classId                       String?                    @map("class_id") @db.Char(36)
  name                          String                     @db.VarChar(255)
  eventName                     String?                    @map("event_name") @db.VarChar(255)
  eventDate                     DateTime?                  @map("event_date") @db.Date
  description                   String?                    @db.Text
  photoSessionName              String?                    @map("photo_session_name") @db.VarChar(255)
  photoDate                     DateTime?                  @map("photo_date") @db.Date
  location                      String?                    @db.VarChar(255)
  photographer                  String?                    @db.VarChar(255)
  photoUrl                      String                     @map("photo_url") @db.VarChar(500)
  thumbnailUrl                  String?                    @map("thumbnail_url") @db.VarChar(500)
  rawGroupPhotoUrl              String?                    @map("raw_group_photo_url") @db.VarChar(500)
  processedGroupPhotoUrl        String?                    @map("processed_group_photo_url") @db.VarChar(500)
  perceptualHash                String?                    @map("perceptual_hash") @db.VarChar(64)
  packageTier                   GroupPhotoPackage          @default(basic) @map("package_tier")
  status                        String                     @default("pending") @db.VarChar(20)
  processingStatus              GroupPhotoProcessingStatus @default(uploaded) @map("processing_status")
  faceDetectionCompleted        Boolean                    @default(false) @map("face_detection_completed")
  individualExtractionCompleted Boolean                    @default(false) @map("individual_extraction_completed")
  totalStudentsDetected         Int                        @default(0) @map("total_students_detected")
  rowCount                      Int                        @default(0) @map("row_count")
  teacherDetected               Boolean                    @default(false) @map("teacher_detected")
  metadata                      Json?
  createdAt                     DateTime                   @default(now()) @map("created_at")
  updatedAt                     DateTime                   @updatedAt @map("updated_at")
  extractions                   GroupPhotoExtraction[]
  class                         Class?                     @relation(fields: [classId], references: [id])
  institution                   Institution                @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  section                       Section?                   @relation(fields: [sectionId], references: [id])

  @@index([institutionId])
  @@index([sectionId])
  @@index([processingStatus])
  @@index([perceptualHash])
  @@index([classId], map: "group_photos_class_id_fkey")
  @@map("group_photos")
}

model GroupPhotoExtraction {
  id                 String     @id @default(uuid()) @db.Char(36)
  groupPhotoId       String     @map("group_photo_id") @db.Char(36)
  studentId          String?    @map("student_id") @db.Char(36)
  rowNumber          Int?       @map("row_number")
  positionInRow      Int?       @map("position_in_row")
  boundingBox        Json?      @map("bounding_box")
  confidenceScore    Decimal?   @map("confidence_score") @db.Decimal(3, 2)
  faceHash           String?    @map("face_hash") @db.VarChar(64)
  matchConfidence    Int?       @map("match_confidence")
  isAutoMatched      Boolean    @default(false) @map("is_auto_matched")
  isRejected         Boolean    @default(false) @map("is_rejected")
  manualLabel        String?    @map("manual_label") @db.VarChar(255)
  individualPhotoUrl String?    @map("individual_photo_url") @db.VarChar(500)
  enhancedPhotoUrl   String?    @map("enhanced_photo_url") @db.VarChar(500)
  framedPhotoUrl     String?    @map("framed_photo_url") @db.VarChar(500)
  yearbookFormatUrl  String?    @map("yearbook_format_url") @db.VarChar(500)
  createdAt          DateTime   @default(now()) @map("created_at")
  groupPhoto         GroupPhoto @relation(fields: [groupPhotoId], references: [id], onDelete: Cascade)
  student            Student?   @relation(fields: [studentId], references: [id])

  @@index([groupPhotoId])
  @@index([studentId])
  @@index([faceHash])
  @@map("group_photo_extractions")
}

model Certificate {
  id                  String             @id @default(uuid()) @db.Char(36)
  institutionId       String             @map("institution_id") @db.Char(36)
  studentId           String             @map("student_id") @db.Char(36)
  templateId          String?            @map("template_id") @db.Char(36)
  certificateType     CertificateType    @map("certificate_type")
  certificateNumber   String?            @map("certificate_number") @db.VarChar(50)
  title               String             @db.VarChar(255)
  description         String?            @db.Text
  achievementDetails  String?            @map("achievement_details") @db.Text
  awardedForPeriod    String?            @map("awarded_for_period") @db.VarChar(100)
  issueDate           DateTime           @map("issue_date") @db.Date
  verificationQrCode  String?            @map("verification_qr_code") @db.Text
  verificationUrl     String?            @map("verification_url") @db.VarChar(500)
  digitalSignatureUrl String?            @map("digital_signature_url") @db.VarChar(500)
  pdfUrl              String?            @map("pdf_url") @db.VarChar(500)
  packageTier         CertificatePackage @default(standard) @map("package_tier")
  status              CertificateStatus  @default(draft)
  emailSentAt         DateTime?          @map("email_sent_at")
  createdAt           DateTime           @default(now()) @map("created_at")
  updatedAt           DateTime           @updatedAt @map("updated_at")
  institution         Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student             Student            @relation(fields: [studentId], references: [id], onDelete: Cascade)
  template            Template?          @relation(fields: [templateId], references: [id])

  @@unique([institutionId, certificateNumber])
  @@index([studentId])
  @@index([certificateType])
  @@index([status])
  @@index([institutionId], map: "certificates_institution_id_fkey")
  @@index([templateId], map: "certificates_template_id_fkey")
  @@map("certificates")
}

model ExamSchedule {
  id            String             @id @default(uuid()) @db.Char(36)
  institutionId String             @map("institution_id") @db.Char(36)
  examName      String             @map("exam_name") @db.VarChar(255)
  examType      ExamType           @default(internal) @map("exam_type")
  academicYear  String?            @map("academic_year") @db.VarChar(20)
  startDate     DateTime           @map("start_date") @db.Date
  endDate       DateTime           @map("end_date") @db.Date
  instructions  String?            @db.Text
  reportingTime DateTime?          @map("reporting_time") @db.Time(0)
  status        ExamScheduleStatus @default(draft)
  createdAt     DateTime           @default(now()) @map("created_at")
  updatedAt     DateTime           @updatedAt @map("updated_at")
  institution   Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  subjects      ExamSubject[]
  hallTickets   HallTicket[]
  marks         Mark[]
  marksheets    Marksheet[]

  @@index([institutionId])
  @@map("exam_schedules")
}

model ExamSubject {
  id              String       @id @default(uuid()) @db.Char(36)
  examScheduleId  String       @map("exam_schedule_id") @db.Char(36)
  subjectName     String       @map("subject_name") @db.VarChar(255)
  subjectCode     String?      @map("subject_code") @db.VarChar(50)
  examDate        DateTime     @map("exam_date") @db.Date
  startTime       DateTime     @map("start_time") @db.Time(0)
  durationMinutes Int          @map("duration_minutes")
  venue           String?      @db.VarChar(255)
  maxMarks        Int?         @map("max_marks")
  createdAt       DateTime     @default(now()) @map("created_at")
  examSchedule    ExamSchedule @relation(fields: [examScheduleId], references: [id], onDelete: Cascade)

  @@index([examScheduleId])
  @@map("exam_subjects")
}

model HallTicket {
  id                String             @id @default(uuid()) @db.Char(36)
  examScheduleId    String             @map("exam_schedule_id") @db.Char(36)
  studentId         String             @map("student_id") @db.Char(36)
  institutionId     String             @map("institution_id") @db.Char(36)
  templateId        String?            @map("template_id") @db.Char(36)
  hallTicketNumber  String?            @map("hall_ticket_number") @db.VarChar(50)
  rollNumberForExam String?            @map("roll_number_for_exam") @db.VarChar(50)
  examCenter        String?            @map("exam_center") @db.VarChar(255)
  seatNumber        String?            @map("seat_number") @db.VarChar(50)
  qrCodeData        String?            @map("qr_code_data") @db.Text
  barcodeData       String?            @map("barcode_data") @db.VarChar(100)
  pdfUrl            String?            @map("pdf_url") @db.VarChar(500)
  status            HallTicketStatus   @default(generated)
  sentVia           HallTicketSentVia? @map("sent_via")
  sentAt            DateTime?          @map("sent_at")
  createdAt         DateTime           @default(now()) @map("created_at")
  updatedAt         DateTime           @updatedAt @map("updated_at")
  examSchedule      ExamSchedule       @relation(fields: [examScheduleId], references: [id], onDelete: Cascade)
  institution       Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student           Student            @relation(fields: [studentId], references: [id], onDelete: Cascade)
  template          Template?          @relation(fields: [templateId], references: [id])

  @@unique([examScheduleId, studentId])
  @@unique([institutionId, hallTicketNumber])
  @@index([studentId])
  @@index([status])
  @@index([institutionId], map: "hall_tickets_institution_id_fkey")
  @@index([templateId], map: "hall_tickets_template_id_fkey")
  @@map("hall_tickets")
}

model CalculationEngine {
  id                   String               @id @default(uuid()) @db.Char(36)
  institutionId        String               @map("institution_id") @db.Char(36)
  academicYear         String               @map("academic_year") @db.VarChar(20)
  cgpaFormula          String               @map("cgpa_formula") @db.Text
  percentageFormula    String               @map("percentage_formula") @db.Text
  percentileFormula    String               @map("percentile_formula") @db.Text
  gradeScale           Json                 @map("grade_scale")
  theoryWeightage      Decimal              @default(70.00) @map("theory_weightage") @db.Decimal(5, 2)
  practicalWeightage   Decimal              @default(30.00) @map("practical_weightage") @db.Decimal(5, 2)
  internalWeightage    Decimal              @default(20.00) @map("internal_weightage") @db.Decimal(5, 2)
  rankCalculationScope RankCalculationScope @default(class_all_sections) @map("rank_calculation_scope")
  graceMarksEnabled    Boolean              @default(false) @map("grace_marks_enabled")
  maxGraceMarks        Int                  @default(0) @map("max_grace_marks")
  graceMarksRules      Json?                @map("grace_marks_rules")
  decimalPrecision     Int                  @default(2) @map("decimal_precision")
  roundingMethod       RoundingMethod       @default(round) @map("rounding_method")
  isActive             Boolean              @default(true) @map("is_active")
  locked               Boolean              @default(false)
  createdAt            DateTime             @default(now()) @map("created_at")
  updatedAt            DateTime             @updatedAt @map("updated_at")
  institution          Institution          @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  marksheets           Marksheet[]

  @@unique([institutionId, academicYear])
  @@index([institutionId])
  @@map("calculation_engines")
}

model Subject {
  id                    String                  @id @default(uuid()) @db.Char(36)
  institutionId         String                  @map("institution_id") @db.Char(36)
  classId               String                  @map("class_id") @db.Char(36)
  subjectName           String                  @map("subject_name") @db.VarChar(255)
  subjectCode           String?                 @map("subject_code") @db.VarChar(50)
  subjectType           SubjectType             @default(theory) @map("subject_type")
  isMandatory           Boolean                 @default(true) @map("is_mandatory")
  createdAt             DateTime                @default(now()) @map("created_at")
  attendanceSessions    AttendanceSession[]
  marks                 Mark[]
  class                 Class                   @relation(fields: [classId], references: [id], onDelete: Cascade)
  institution           Institution             @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  visionariumTestSeries VisionariumTestSeries[]

  @@index([classId])
  @@index([institutionId], map: "subjects_institution_id_fkey")
  @@map("subjects")
}

model Mark {
  id                     String       @id @default(uuid()) @db.Char(36)
  studentId              String       @map("student_id") @db.Char(36)
  subjectId              String       @map("subject_id") @db.Char(36)
  examScheduleId         String       @map("exam_schedule_id") @db.Char(36)
  theoryMaxMarks         Int?         @map("theory_max_marks")
  theoryObtainedMarks    Decimal?     @map("theory_obtained_marks") @db.Decimal(5, 2)
  practicalMaxMarks      Int?         @map("practical_max_marks")
  practicalObtainedMarks Decimal?     @map("practical_obtained_marks") @db.Decimal(5, 2)
  internalMaxMarks       Int?         @map("internal_max_marks")
  internalObtainedMarks  Decimal?     @map("internal_obtained_marks") @db.Decimal(5, 2)
  totalMaxMarks          Int?         @map("total_max_marks")
  totalObtainedMarks     Decimal?     @map("total_obtained_marks") @db.Decimal(5, 2)
  percentage             Decimal?     @db.Decimal(5, 2)
  grade                  String?      @db.VarChar(10)
  status                 MarkStatus   @default(draft)
  enteredBy              String?      @map("entered_by") @db.Char(36)
  approvedBy             String?      @map("approved_by") @db.Char(36)
  createdAt              DateTime     @default(now()) @map("created_at")
  updatedAt              DateTime     @updatedAt @map("updated_at")
  examSchedule           ExamSchedule @relation(fields: [examScheduleId], references: [id], onDelete: Cascade)
  student                Student      @relation(fields: [studentId], references: [id], onDelete: Cascade)
  subject                Subject      @relation(fields: [subjectId], references: [id], onDelete: Cascade)

  @@unique([studentId, subjectId, examScheduleId])
  @@index([studentId])
  @@index([examScheduleId])
  @@index([status])
  @@index([subjectId], map: "marks_subject_id_fkey")
  @@map("marks")
}

model Marksheet {
  id                        String            @id @default(uuid()) @db.Char(36)
  studentId                 String            @map("student_id") @db.Char(36)
  examScheduleId            String            @map("exam_schedule_id") @db.Char(36)
  institutionId             String            @map("institution_id") @db.Char(36)
  templateId                String            @map("template_id") @db.Char(36)
  calculationEngineId       String            @map("calculation_engine_id") @db.Char(36)
  totalPercentage           Decimal?          @map("total_percentage") @db.Decimal(5, 2)
  cgpa                      Decimal?          @db.Decimal(4, 2)
  grade                     String?           @db.VarChar(10)
  classRank                 Int?              @map("class_rank")
  sectionRank               Int?              @map("section_rank")
  overallRank               Int?              @map("overall_rank")
  percentile                Decimal?          @db.Decimal(5, 2)
  totalStudentsInComparison Int?              @map("total_students_in_comparison")
  pdfUrl                    String?           @map("pdf_url") @db.VarChar(500)
  digitalSignatureUrl       String?           @map("digital_signature_url") @db.VarChar(500)
  status                    MarksheetStatus   @default(draft)
  publishedAt               DateTime?         @map("published_at")
  createdAt                 DateTime          @default(now()) @map("created_at")
  updatedAt                 DateTime          @updatedAt @map("updated_at")
  calculationEngine         CalculationEngine @relation(fields: [calculationEngineId], references: [id])
  examSchedule              ExamSchedule      @relation(fields: [examScheduleId], references: [id], onDelete: Cascade)
  institution               Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student                   Student           @relation(fields: [studentId], references: [id], onDelete: Cascade)
  template                  Template          @relation(fields: [templateId], references: [id])

  @@unique([studentId, examScheduleId])
  @@index([studentId])
  @@index([examScheduleId])
  @@index([calculationEngineId], map: "marksheets_calculation_engine_id_fkey")
  @@index([institutionId], map: "marksheets_institution_id_fkey")
  @@index([templateId], map: "marksheets_template_id_fkey")
  @@map("marksheets")
}

model LibraryCard {
  id                  String            @id @default(uuid()) @db.Char(36)
  studentId           String            @map("student_id") @db.Char(36)
  institutionId       String            @map("institution_id") @db.Char(36)
  templateId          String            @map("template_id") @db.Char(36)
  libraryCardNumber   String            @map("library_card_number") @db.VarChar(50)
  issueDate           DateTime          @map("issue_date") @db.Date
  expiryDate          DateTime          @map("expiry_date") @db.Date
  maxBooksAllowed     Int               @default(3) @map("max_books_allowed")
  borrowingPeriodDays Int               @default(14) @map("borrowing_period_days")
  qrCodeData          String?           @map("qr_code_data") @db.Text
  barcodeData         String?           @map("barcode_data") @db.VarChar(100)
  pdfUrl              String?           @map("pdf_url") @db.VarChar(500)
  status              LibraryCardStatus @default(active)
  createdAt           DateTime          @default(now()) @map("created_at")
  updatedAt           DateTime          @updatedAt @map("updated_at")
  institution         Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student             Student           @relation(fields: [studentId], references: [id], onDelete: Cascade)
  template            Template          @relation(fields: [templateId], references: [id])

  @@unique([studentId, institutionId])
  @@unique([institutionId, libraryCardNumber])
  @@index([studentId])
  @@index([status])
  @@index([institutionId], map: "library_cards_institution_id_fkey")
  @@index([templateId], map: "library_cards_template_id_fkey")
  @@map("library_cards")
}

model TransferCertificate {
  id                    String                    @id @default(uuid()) @db.Char(36)
  studentId             String                    @map("student_id") @db.Char(36)
  institutionId         String                    @map("institution_id") @db.Char(36)
  templateId            String?                   @map("template_id") @db.Char(36)
  tcSerialNumber        String                    @map("tc_serial_number") @db.VarChar(50)
  admissionDate         DateTime                  @map("admission_date") @db.Date
  leavingDate           DateTime                  @map("leaving_date") @db.Date
  lastClassStudied      String?                   @map("last_class_studied") @db.VarChar(100)
  reasonForLeaving      String?                   @map("reason_for_leaving") @db.Text
  conductRemarks        ConductRemarks            @default(good) @map("conduct_remarks")
  characterRemarks      String?                   @map("character_remarks") @db.Text
  feeClearanceStatus    FeeClearanceStatus        @default(pending) @map("fee_clearance_status")
  outstandingAmount     Decimal                   @default(0.00) @map("outstanding_amount") @db.Decimal(10, 2)
  totalWorkingDays      Int?                      @map("total_working_days")
  totalPresentDays      Int?                      @map("total_present_days")
  attendancePercentage  Decimal?                  @map("attendance_percentage") @db.Decimal(5, 2)
  principalSignatureUrl String?                   @map("principal_signature_url") @db.VarChar(500)
  sealUrl               String?                   @map("seal_url") @db.VarChar(500)
  pdfUrl                String?                   @map("pdf_url") @db.VarChar(500)
  issuedBy              String?                   @map("issued_by") @db.Char(36)
  issuedAt              DateTime?                 @map("issued_at")
  duplicateIssued       Boolean                   @default(false) @map("duplicate_issued")
  originalTcId          String?                   @map("original_tc_id") @db.Char(36)
  status                TransferCertificateStatus @default(draft)
  locked                Boolean                   @default(false)
  createdAt             DateTime                  @default(now()) @map("created_at")
  updatedAt             DateTime                  @updatedAt @map("updated_at")
  institution           Institution               @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student               Student                   @relation(fields: [studentId], references: [id], onDelete: Cascade)
  template              Template?                 @relation(fields: [templateId], references: [id])

  @@unique([institutionId, tcSerialNumber])
  @@index([studentId])
  @@index([tcSerialNumber])
  @@index([status])
  @@index([institutionId], map: "transfer_certificates_institution_id_fkey")
  @@index([templateId], map: "transfer_certificates_template_id_fkey")
  @@map("transfer_certificates")
}

model Approval {
  id              String             @id @default(uuid()) @db.Char(36)
  institutionId   String             @map("institution_id") @db.Char(36)
  entityType      ApprovalEntityType @map("entity_type")
  entityId        String             @map("entity_id") @db.Char(36)
  submittedBy     String?            @map("submitted_by") @db.Char(36)
  reviewedBy      String?            @map("reviewed_by") @db.Char(36)
  status          ApprovalStatus
  reviewNotes     String?            @map("review_notes") @db.Text
  submittedAt     DateTime           @default(now()) @map("submitted_at")
  reviewedAt      DateTime?          @map("reviewed_at")
  institution     Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  reviewedByUser  User?              @relation("ReviewedApprovals", fields: [reviewedBy], references: [id])
  submittedByUser User?              @relation("SubmittedApprovals", fields: [submittedBy], references: [id])

  @@index([institutionId])
  @@index([entityType, entityId])
  @@index([status])
  @@index([reviewedBy], map: "approvals_reviewed_by_fkey")
  @@index([submittedBy], map: "approvals_submitted_by_fkey")
  @@map("approvals")
}

model AttendanceLog {
  id              String      @id @default(uuid()) @db.Char(36)
  studentId       String      @map("student_id") @db.Char(36)
  institutionId   String      @map("institution_id") @db.Char(36)
  attendanceDate  DateTime    @map("attendance_date") @db.Date
  attendanceTime  DateTime    @map("attendance_time") @db.Time(0)
  scanType        ScanType    @default(entry) @map("scan_type")
  scannerId       String?     @map("scanner_id") @db.VarChar(100)
  scannerLocation String?     @map("scanner_location") @db.VarChar(255)
  qrSource        QrSource    @default(id_card) @map("qr_source")
  notes           String?     @db.Text
  timestamp       DateTime    @default(now())
  institution     Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student         Student     @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@index([studentId])
  @@index([institutionId, attendanceDate])
  @@index([attendanceDate])
  @@map("attendance_logs")
}

model JobExecution {
  id              String      @id @default(uuid()) @db.Char(36)
  jobId           String      @unique @map("job_id") @db.VarChar(255)
  jobType         JobType     @map("job_type")
  institutionId   String      @map("institution_id") @db.Char(36)
  sectionId       String?     @map("section_id") @db.Char(36)
  initiatedBy     String?     @map("initiated_by") @db.Char(36)
  status          JobStatus   @default(queued)
  progress        Int         @default(0)
  totalItems      Int         @default(0) @map("total_items")
  processedItems  Int         @default(0) @map("processed_items")
  successfulItems Int         @default(0) @map("successful_items")
  failedItems     Int         @default(0) @map("failed_items")
  errorMessage    String?     @map("error_message") @db.Text
  errorStack      String?     @map("error_stack") @db.Text
  resultData      Json?       @map("result_data")
  startedAt       DateTime?   @map("started_at")
  completedAt     DateTime?   @map("completed_at")
  durationSeconds Int?        @map("duration_seconds")
  createdAt       DateTime    @default(now()) @map("created_at")
  institution     Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([jobId])
  @@index([status])
  @@index([institutionId])
  @@index([jobType, status])
  @@map("job_executions")
}

model AuditLog {
  id            String       @id @default(uuid()) @db.Char(36)
  action        String       @db.VarChar(100)
  userId        String?      @map("user_id") @db.Char(36)
  institutionId String?      @map("institution_id") @db.Char(36)
  entityType    String       @map("entity_type") @db.VarChar(100)
  entityId      String       @map("entity_id") @db.Char(36)
  changes       Json?
  ipAddress     String?      @map("ip_address") @db.VarChar(50)
  userAgent     String?      @map("user_agent") @db.Text
  requestId     String?      @map("request_id") @db.VarChar(100)
  timestamp     DateTime     @default(now())
  institution   Institution? @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  user          User?        @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([institutionId])
  @@index([entityType, entityId])
  @@index([timestamp])
  @@map("audit_logs")
}

model ImageCache {
  id             String              @id @default(uuid()) @db.Char(36)
  perceptualHash String              @unique @map("perceptual_hash") @db.VarChar(64)
  qualityScore   Decimal             @map("quality_score") @db.Decimal(3, 2)
  isApproved     Boolean             @map("is_approved")
  issues         Json?
  analysisMethod ImageAnalysisMethod @map("analysis_method")
  hitCount       Int                 @default(1) @map("hit_count")
  lastAccessed   DateTime            @default(now()) @updatedAt @map("last_accessed")
  expiresAt      DateTime            @map("expires_at")
  createdAt      DateTime            @default(now()) @map("created_at")

  @@index([perceptualHash])
  @@index([expiresAt])
  @@map("image_cache")
}

model SystemConfig {
  id          String         @id @default(uuid()) @db.Char(36)
  configKey   String         @unique @map("config_key") @db.VarChar(100)
  configValue String         @map("config_value") @db.Text
  dataType    ConfigDataType @default(string) @map("data_type")
  description String?        @db.Text
  isPublic    Boolean        @default(false) @map("is_public")
  createdAt   DateTime       @default(now()) @map("created_at")
  updatedAt   DateTime       @updatedAt @map("updated_at")

  @@index([configKey])
  @@map("system_config")
}

model Portfolio {
  id            String             @id @default(uuid()) @db.Char(36)
  studentId     String             @unique @map("student_id") @db.Char(36)
  institutionId String             @map("institution_id") @db.Char(36)
  templateId    String?            @map("template_id") @db.Char(36)
  title         String             @db.VarChar(255)
  bio           String?            @db.Text
  slug          String             @unique @db.VarChar(100)
  theme         PortfolioTheme     @default(modern)
  themeConfig   Json?              @map("theme_config")
  customDomain  String?            @map("custom_domain") @db.VarChar(100)
  isPublic      Boolean            @default(true) @map("is_public")
  status        PortfolioStatus    @default(draft)
  viewCount     Int                @default(0) @map("view_count")
  publishedAt   DateTime?          @map("published_at")
  createdAt     DateTime           @default(now()) @map("created_at")
  updatedAt     DateTime           @updatedAt @map("updated_at")
  sections      PortfolioSection[]
  institution   Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student       Student            @relation(fields: [studentId], references: [id], onDelete: Cascade)
  template      Template?          @relation(fields: [templateId], references: [id])

  @@index([institutionId])
  @@index([slug])
  @@index([status])
  @@index([templateId], map: "portfolios_template_id_fkey")
  @@map("portfolios")
}

model PortfolioSection {
  id          String               @id @default(uuid()) @db.Char(36)
  portfolioId String               @map("portfolio_id") @db.Char(36)
  type        PortfolioSectionType
  title       String               @db.VarChar(100)
  content     Json
  order       Int                  @default(0)
  isVisible   Boolean              @default(true) @map("is_visible")
  createdAt   DateTime             @default(now()) @map("created_at")
  updatedAt   DateTime             @updatedAt @map("updated_at")
  portfolio   Portfolio            @relation(fields: [portfolioId], references: [id], onDelete: Cascade)

  @@index([portfolioId])
  @@map("portfolio_sections")
}

model Notification {
  id            String           @id @default(uuid()) @db.Char(36)
  userId        String           @map("user_id") @db.Char(36)
  institutionId String           @map("institution_id") @db.Char(36)
  title         String           @db.VarChar(100)
  message       String           @db.VarChar(500)
  type          NotificationType @default(info)
  actionUrl     String?          @map("action_url") @db.VarChar(500)
  metadata      Json?
  isRead        Boolean          @default(false) @map("is_read")
  readAt        DateTime?        @map("read_at")
  createdAt     DateTime         @default(now()) @map("created_at")
  institution   Institution      @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  user          User             @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, isRead])
  @@index([institutionId])
  @@map("notifications")
}

model NotificationTemplate {
  id            String                   @id @default(uuid()) @db.Char(36)
  institutionId String                   @map("institution_id") @db.Char(36)
  name          String                   @db.VarChar(100)
  type          NotificationTemplateType
  subject       String?                  @db.VarChar(255)
  content       String                   @db.Text
  variables     Json
  isActive      Boolean                  @default(true) @map("is_active")
  createdAt     DateTime                 @default(now()) @map("created_at")
  updatedAt     DateTime                 @updatedAt @map("updated_at")
  institution   Institution              @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@map("notification_templates")
}

model NotificationLog {
  id            String                   @id @default(uuid()) @db.Char(36)
  institutionId String                   @map("institution_id") @db.Char(36)
  type          NotificationTemplateType
  recipient     String                   @db.VarChar(255)
  recipientName String?                  @map("recipient_name") @db.VarChar(255)
  subject       String?                  @db.VarChar(255)
  content       String                   @db.Text
  status        NotificationLogStatus    @default(pending)
  messageId     String?                  @map("message_id") @db.VarChar(100)
  errorMessage  String?                  @map("error_message") @db.Text
  metadata      Json?
  priority      String?                  @db.VarChar(20)
  sentAt        DateTime?                @map("sent_at")
  createdAt     DateTime                 @default(now()) @map("created_at")
  institution   Institution              @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([type, status])
  @@map("notification_logs")
}

model ApprovalWorkflow {
  id            String            @id @default(uuid()) @db.Char(36)
  institutionId String            @map("institution_id") @db.Char(36)
  name          String            @db.VarChar(100)
  type          String            @db.VarChar(50)
  description   String?           @db.VarChar(500)
  steps         Json
  isActive      Boolean           @default(true) @map("is_active")
  createdAt     DateTime          @default(now()) @map("created_at")
  updatedAt     DateTime          @updatedAt @map("updated_at")
  requests      ApprovalRequest[]
  institution   Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([type, isActive])
  @@map("approval_workflows")
}

model ApprovalRequest {
  id            String           @id @default(uuid()) @db.Char(36)
  institutionId String           @map("institution_id") @db.Char(36)
  requesterId   String           @map("requester_id") @db.Char(36)
  workflowId    String           @map("workflow_id") @db.Char(36)
  type          String           @db.VarChar(50)
  title         String           @db.VarChar(255)
  description   String?          @db.Text
  entityType    String?          @map("entity_type") @db.VarChar(50)
  entityId      String?          @map("entity_id") @db.Char(36)
  metadata      Json?
  priority      ApprovalPriority @default(normal)
  status        ApprovalStatus   @default(pending)
  currentStep   Int              @default(1) @map("current_step")
  totalSteps    Int              @map("total_steps")
  dueDate       DateTime?        @map("due_date")
  completedAt   DateTime?        @map("completed_at")
  createdAt     DateTime         @default(now()) @map("created_at")
  updatedAt     DateTime         @updatedAt @map("updated_at")
  institution   Institution      @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  requester     User             @relation(fields: [requesterId], references: [id], onDelete: Cascade)
  workflow      ApprovalWorkflow @relation(fields: [workflowId], references: [id], onDelete: Cascade)
  steps         ApprovalStep[]

  @@index([institutionId])
  @@index([requesterId])
  @@index([status])
  @@index([workflowId], map: "approval_requests_workflow_id_fkey")
  @@map("approval_requests")
}

model ApprovalStep {
  id             String             @id @default(uuid()) @db.Char(36)
  requestId      String             @map("request_id") @db.Char(36)
  stepNumber     Int                @map("step_number")
  name           String             @db.VarChar(100)
  approverRole   String             @map("approver_role") @db.VarChar(50)
  approverUserId String?            @map("approver_user_id") @db.Char(36)
  isRequired     Boolean            @default(true) @map("is_required")
  status         ApprovalStepStatus @default(pending)
  approverId     String?            @map("approver_id") @db.Char(36)
  comments       String?            @db.Text
  conditions     Json?
  processedAt    DateTime?          @map("processed_at")
  createdAt      DateTime           @default(now()) @map("created_at")
  approver       User?              @relation(fields: [approverId], references: [id])
  request        ApprovalRequest    @relation(fields: [requestId], references: [id], onDelete: Cascade)

  @@index([requestId])
  @@index([approverId], map: "approval_steps_approver_id_fkey")
  @@map("approval_steps")
}

model AttendanceSession {
  id            String                  @id @default(uuid()) @db.Char(36)
  institutionId String                  @map("institution_id") @db.Char(36)
  classId       String                  @map("class_id") @db.Char(36)
  sectionId     String                  @map("section_id") @db.Char(36)
  subjectId     String?                 @map("subject_id") @db.Char(36)
  createdById   String                  @map("created_by_id") @db.Char(36)
  date          DateTime                @db.Date
  type          AttendanceType          @default(class)
  startTime     String                  @map("start_time") @db.VarChar(5)
  endTime       String?                 @map("end_time") @db.VarChar(5)
  location      String?                 @db.VarChar(255)
  notes         String?                 @db.VarChar(500)
  status        AttendanceSessionStatus @default(open)
  closedAt      DateTime?               @map("closed_at")
  createdAt     DateTime                @default(now()) @map("created_at")
  records       AttendanceRecord[]
  class         Class                   @relation(fields: [classId], references: [id], onDelete: Cascade)
  createdBy     User                    @relation(fields: [createdById], references: [id])
  institution   Institution             @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  section       Section                 @relation(fields: [sectionId], references: [id], onDelete: Cascade)
  subject       Subject?                @relation(fields: [subjectId], references: [id])

  @@index([institutionId])
  @@index([sectionId, date])
  @@index([classId], map: "attendance_sessions_class_id_fkey")
  @@index([createdById], map: "attendance_sessions_created_by_id_fkey")
  @@index([subjectId], map: "attendance_sessions_subject_id_fkey")
  @@map("attendance_sessions")
}

model AttendanceRecord {
  id            String                 @id @default(uuid()) @db.Char(36)
  sessionId     String                 @map("session_id") @db.Char(36)
  studentId     String                 @map("student_id") @db.Char(36)
  status        AttendanceRecordStatus @default(present)
  remarks       String?                @db.VarChar(200)
  arrivalTime   String?                @map("arrival_time") @db.VarChar(5)
  checkInMethod String?                @map("check_in_method") @db.VarChar(20)
  markedById    String?                @map("marked_by_id") @db.Char(36)
  markedAt      DateTime?              @map("marked_at")
  createdAt     DateTime               @default(now()) @map("created_at")
  markedBy      User?                  @relation(fields: [markedById], references: [id])
  session       AttendanceSession      @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  student       Student                @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@unique([sessionId, studentId])
  @@index([studentId])
  @@index([markedById], map: "attendance_records_marked_by_id_fkey")
  @@map("attendance_records")
}

model StudentFormProgress {
  id            String      @id @default(uuid()) @db.Char(36)
  studentId     String      @unique @map("student_id") @db.Char(36)
  institutionId String      @map("institution_id") @db.Char(36)
  tabAcademic   Boolean     @default(false) @map("tab_academic")
  tabPersonal   Boolean     @default(false) @map("tab_personal")
  tabPhoto      Boolean     @default(false) @map("tab_photo")
  tabFamily     Boolean     @default(false) @map("tab_family")
  tabContact    Boolean     @default(false) @map("tab_contact")
  tabOther      Boolean     @default(false) @map("tab_other")
  activeTab     FormTab     @default(academic) @map("active_tab")
  completedAt   DateTime?   @map("completed_at")
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")
  institution   Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student       Student     @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@index([institutionId], map: "student_form_progress_institution_id_fkey")
  @@map("student_form_progress")
}

model StudentOnboardingToken {
  id            String         @id @default(uuid()) @db.Char(36)
  studentId     String         @unique @map("student_id") @db.Char(36)
  institutionId String         @map("institution_id") @db.Char(36)
  token         String         @unique @db.VarChar(64)
  mode          OnboardingMode @default(selfservice)
  expiresAt     DateTime       @map("expires_at")
  usedAt        DateTime?      @map("used_at")
  usedByIp      String?        @map("used_by_ip") @db.VarChar(50)
  revokedAt     DateTime?      @map("revoked_at")
  createdAt     DateTime       @default(now()) @map("created_at")
  institution   Institution    @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student       Student        @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@index([token])
  @@index([studentId])
  @@index([institutionId], map: "student_onboarding_tokens_institution_id_fkey")
  @@map("student_onboarding_tokens")
}

model VisionariumIssue {
  id            String               @id @default(uuid()) @db.Char(36)
  issueCode     String               @unique @map("issue_code") @db.VarChar(50)
  coverImageUrl String?              @map("cover_image_url") @db.VarChar(500)
  publishDate   DateTime             @map("publish_date") @db.Date
  createdAt     DateTime             @default(now()) @map("created_at")
  updatedAt     DateTime             @updatedAt @map("updated_at")
  number        Int?
  titleEnglish  String               @map("title_english") @db.VarChar(255)
  titleHindi    String?              @map("title_hindi") @db.VarChar(255)
  volume        Int?
  articles      VisionariumArticle[]

  @@index([issueCode])
  @@map("visionarium_issues")
}

model VisionariumArticle {
  id              String                   @id @default(uuid()) @db.Char(36)
  institutionId   String?                  @map("institution_id") @db.Char(36)
  authorUserId    String?                  @map("author_user_id") @db.Char(36)
  authorStudentId String?                  @map("author_student_id") @db.Char(36)
  issueId         String?                  @map("issue_id") @db.Char(36)
  title           String                   @db.VarChar(255)
  slug            String                   @unique @db.VarChar(255)
  body            String                   @db.LongText
  summary         String?                  @db.Text
  language        VisionariumLanguage      @default(en)
  category        VisionariumSubject       @default(science)
  status          VisionariumArticleStatus @default(draft)
  publishedAt     DateTime?                @map("published_at")
  createdAt       DateTime                 @default(now()) @map("created_at")
  updatedAt       DateTime                 @updatedAt @map("updated_at")
  articleType     VisionariumArticleType?  @map("article_type")
  perspective     VisionariumPerspective?
  translationOfId String?                  @map("translation_of_id") @db.Char(36)
  socialPosts     SocialPost[]
  authorStudent   Student?                 @relation("StudentArticles", fields: [authorStudentId], references: [id])
  authorUser      User?                    @relation("UserArticles", fields: [authorUserId], references: [id])
  institution     Institution?             @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  issue           VisionariumIssue?        @relation(fields: [issueId], references: [id])
  translationOf   VisionariumArticle?      @relation("ArticleTranslations", fields: [translationOfId], references: [id])
  translations    VisionariumArticle[]     @relation("ArticleTranslations")
  submissions     VisionariumSubmission[]

  @@unique([translationOfId, language])
  @@index([institutionId])
  @@index([authorUserId])
  @@index([category])
  @@index([status])
  @@index([language])
  @@index([authorStudentId], map: "visionarium_articles_author_student_id_fkey")
  @@index([issueId], map: "visionarium_articles_issue_id_fkey")
  @@map("visionarium_articles")
}

model VisionariumSchoolSubscription {
  id            String          @id @default(uuid()) @db.Char(36)
  institutionId String          @map("institution_id") @db.Char(36)
  plan          VisionariumPlan @default(basic)
  startsAt      DateTime        @map("starts_at")
  endsAt        DateTime        @map("ends_at")
  isActive      Boolean         @default(true) @map("is_active")
  createdAt     DateTime        @default(now()) @map("created_at")
  updatedAt     DateTime        @updatedAt @map("updated_at")
  institution   Institution     @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([isActive])
  @@map("visionarium_school_subscriptions")
}

model VisionariumTestSeries {
  id            String                   @id @default(uuid()) @db.Char(36)
  institutionId String                   @map("institution_id") @db.Char(36)
  title         String                   @db.VarChar(255)
  description   String?                  @db.Text
  classId       String?                  @map("class_id") @db.Char(36)
  subjectId     String?                  @map("subject_id") @db.Char(36)
  language      VisionariumLanguage      @default(en)
  totalMarks    Int                      @map("total_marks")
  metadata      Json?
  createdAt     DateTime                 @default(now()) @map("created_at")
  updatedAt     DateTime                 @updatedAt @map("updated_at")
  attempts      VisionariumTestAttempt[]
  class         Class?                   @relation(fields: [classId], references: [id])
  institution   Institution              @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  subject       Subject?                 @relation(fields: [subjectId], references: [id])

  @@index([institutionId])
  @@index([classId])
  @@index([subjectId], map: "visionarium_test_series_subject_id_fkey")
  @@map("visionarium_test_series")
}

model VisionariumTestAttempt {
  id            String                @id @default(uuid()) @db.Char(36)
  testSeriesId  String                @map("test_series_id") @db.Char(36)
  studentId     String                @map("student_id") @db.Char(36)
  scoreObtained Decimal?              @map("score_obtained") @db.Decimal(5, 2)
  startedAt     DateTime              @default(now()) @map("started_at")
  completedAt   DateTime?             @map("completed_at")
  responseData  Json?                 @map("response_data")
  createdAt     DateTime              @default(now()) @map("created_at")
  updatedAt     DateTime              @updatedAt @map("updated_at")
  student       Student               @relation("StudentTestAttempts", fields: [studentId], references: [id], onDelete: Cascade)
  testSeries    VisionariumTestSeries @relation(fields: [testSeriesId], references: [id], onDelete: Cascade)

  @@index([testSeriesId])
  @@index([studentId])
  @@map("visionarium_test_attempts")
}

model VisionariumSubmission {
  id                String                      @id @default(uuid()) @db.Char(36)
  institutionId     String                      @map("institution_id") @db.Char(36)
  submittedByUserId String                      @map("submitted_by_user_id") @db.Char(36)
  studentId         String?                     @map("student_id") @db.Char(36)
  title             String                      @db.VarChar(255)
  submissionType    VisionariumSubmissionType   @default(article) @map("submission_type")
  body              String?                     @db.LongText
  contentUrl        String?                     @map("content_url") @db.VarChar(500)
  status            VisionariumSubmissionStatus @default(submitted)
  linkedArticleId   String?                     @map("linked_article_id") @db.Char(36)
  createdAt         DateTime                    @default(now()) @map("created_at")
  updatedAt         DateTime                    @updatedAt @map("updated_at")
  institution       Institution                 @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  linkedArticle     VisionariumArticle?         @relation(fields: [linkedArticleId], references: [id])
  student           Student?                    @relation("StudentSubmissions", fields: [studentId], references: [id])
  submittedBy       User                        @relation("UserSubmissions", fields: [submittedByUserId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([submittedByUserId])
  @@index([status])
  @@index([linkedArticleId], map: "visionarium_submissions_linked_article_id_fkey")
  @@index([studentId], map: "visionarium_submissions_student_id_fkey")
  @@map("visionarium_submissions")
}

model SocialRelationship {
  id                String                 @id @default(uuid()) @db.Char(36)
  fromUserId        String                 @map("from_user_id") @db.Char(36)
  toUserId          String                 @map("to_user_id") @db.Char(36)
  relationshipType  SocialRelationshipType @map("relationship_type")
  institutionId     String?                @map("institution_id") @db.Char(36)
  sinceAcademicYear String?                @map("since_academic_year") @db.VarChar(20)
  createdAt         DateTime               @default(now()) @map("created_at")
  updatedAt         DateTime               @updatedAt @map("updated_at")
  fromUser          User                   @relation("RelationshipsFrom", fields: [fromUserId], references: [id], onDelete: Cascade)
  institution       Institution?           @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  toUser            User                   @relation("RelationshipsTo", fields: [toUserId], references: [id], onDelete: Cascade)

  @@unique([fromUserId, toUserId, relationshipType])
  @@index([fromUserId])
  @@index([toUserId])
  @@index([institutionId])
  @@map("social_relationships")
}

model SocialSaathiLink {
  id              String           @id @default(uuid()) @db.Char(36)
  requesterUserId String           @map("requester_user_id") @db.Char(36)
  targetUserId    String           @map("target_user_id") @db.Char(36)
  status          SaathiLinkStatus @default(pending)
  context         SaathiContext    @default(other)
  message         String?          @db.VarChar(255)
  createdAt       DateTime         @default(now()) @map("created_at")
  updatedAt       DateTime         @updatedAt @map("updated_at")
  requester       User             @relation("SaathiSent", fields: [requesterUserId], references: [id], onDelete: Cascade)
  target          User             @relation("SaathiReceived", fields: [targetUserId], references: [id], onDelete: Cascade)

  @@unique([requesterUserId, targetUserId])
  @@index([requesterUserId])
  @@index([targetUserId])
  @@index([status])
  @@map("social_saathi_links")
}

model SocialPost {
  id              String              @id @default(uuid()) @db.Char(36)
  institutionId   String?             @map("institution_id") @db.Char(36)
  authorUserId    String              @map("author_user_id") @db.Char(36)
  authorStudentId String?             @map("author_student_id") @db.Char(36)
  scope           SocialPostScope     @default(institution_only)
  classId         String?             @map("class_id") @db.Char(36)
  sectionId       String?             @map("section_id") @db.Char(36)
  title           String?             @db.VarChar(255)
  body            String              @db.Text
  mediaUrl        String?             @map("media_url") @db.VarChar(500)
  linkedArticleId String?             @map("linked_article_id") @db.Char(36)
  createdAt       DateTime            @default(now()) @map("created_at")
  updatedAt       DateTime            @updatedAt @map("updated_at")
  comments        SocialComment[]
  authorStudent   Student?            @relation("StudentPosts", fields: [authorStudentId], references: [id])
  authorUser      User                @relation("UserPosts", fields: [authorUserId], references: [id], onDelete: Cascade)
  class           Class?              @relation(fields: [classId], references: [id])
  institution     Institution?        @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  linkedArticle   VisionariumArticle? @relation(fields: [linkedArticleId], references: [id])
  section         Section?            @relation(fields: [sectionId], references: [id])
  reactions       SocialReaction[]

  @@index([institutionId])
  @@index([authorUserId])
  @@index([scope])
  @@index([classId])
  @@index([createdAt])
  @@index([authorStudentId], map: "social_posts_author_student_id_fkey")
  @@index([linkedArticleId], map: "social_posts_linked_article_id_fkey")
  @@index([sectionId], map: "social_posts_section_id_fkey")
  @@map("social_posts")
}

model SocialComment {
  id           String     @id @default(uuid()) @db.Char(36)
  postId       String     @map("post_id") @db.Char(36)
  authorUserId String     @map("author_user_id") @db.Char(36)
  body         String     @db.Text
  createdAt    DateTime   @default(now()) @map("created_at")
  updatedAt    DateTime   @updatedAt @map("updated_at")
  authorUser   User       @relation("UserComments", fields: [authorUserId], references: [id], onDelete: Cascade)
  post         SocialPost @relation(fields: [postId], references: [id], onDelete: Cascade)

  @@index([postId])
  @@index([authorUserId])
  @@map("social_comments")
}

model SocialReaction {
  id           String             @id @default(uuid()) @db.Char(36)
  postId       String             @map("post_id") @db.Char(36)
  userId       String             @map("user_id") @db.Char(36)
  reactionType SocialReactionType @default(prerna) @map("reaction_type")
  createdAt    DateTime           @default(now()) @map("created_at")
  post         SocialPost         @relation(fields: [postId], references: [id], onDelete: Cascade)
  user         User               @relation("UserReactions", fields: [userId], references: [id], onDelete: Cascade)

  @@unique([postId, userId])
  @@index([postId])
  @@index([userId])
  @@map("social_reactions")
}

model Session {
  id        String   @id
  expiresAt DateTime
  token     String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  ipAddress String?
  userAgent String?
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId], map: "sessions_userId_fkey")
  @@map("sessions")
}

model Account {
  id                    String    @id
  accountId             String
  providerId            String
  userId                String
  accessToken           String?
  refreshToken          String?
  idToken               String?
  accessTokenExpiresAt  DateTime?
  refreshTokenExpiresAt DateTime?
  scope                 String?
  password              String?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt
  user                  User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([accountId, providerId])
  @@index([userId], map: "accounts_userId_fkey")
  @@map("accounts")
}

model Verification {
  id         String    @id @default(uuid()) @db.Char(36)
  identifier String
  value      String    @db.Text
  expiresAt  DateTime
  createdAt  DateTime? @default(now())
  updatedAt  DateTime? @updatedAt

  @@index([identifier])
  @@map("verifications")
}

model OauthApplication {
  id           String             @id @default(uuid()) @db.Char(36)
  name         String             @db.VarChar(255)
  icon         String?            @db.VarChar(500)
  metadata     String?            @db.Text
  clientId     String             @unique @map("client_id") @db.VarChar(255)
  clientSecret String?            @map("client_secret") @db.VarChar(512)
  redirectUrls String             @map("redirect_urls") @db.Text
  type         String             @default("web") @db.VarChar(50)
  disabled     Boolean            @default(false)
  userId       String?            @map("user_id") @db.Char(36)
  createdAt    DateTime           @default(now()) @map("created_at")
  updatedAt    DateTime           @updatedAt @map("updated_at")
  accessTokens OauthAccessToken[]
  user         User?              @relation(fields: [userId], references: [id], onDelete: Cascade)
  consents     OauthConsent[]

  @@index([userId])
  @@map("oauth_applications")
}

model OauthAccessToken {
  id                    String           @id @default(uuid()) @db.Char(36)
  accessToken           String           @unique @map("access_token") @db.VarChar(512)
  refreshToken          String           @unique @map("refresh_token") @db.VarChar(512)
  accessTokenExpiresAt  DateTime         @map("access_token_expires_at")
  refreshTokenExpiresAt DateTime         @map("refresh_token_expires_at")
  clientId              String           @map("client_id") @db.VarChar(255)
  userId                String?          @map("user_id") @db.Char(36)
  scopes                String           @db.Text
  createdAt             DateTime         @default(now()) @map("created_at")
  updatedAt             DateTime         @updatedAt @map("updated_at")
  application           OauthApplication @relation(fields: [clientId], references: [clientId], onDelete: Cascade)
  user                  User?            @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([clientId])
  @@index([userId])
  @@map("oauth_access_tokens")
}

model OauthConsent {
  id           String           @id @default(uuid()) @db.Char(36)
  clientId     String           @map("client_id") @db.VarChar(255)
  userId       String           @map("user_id") @db.Char(36)
  scopes       String           @db.Text
  consentGiven Boolean          @default(true) @map("consent_given")
  createdAt    DateTime         @default(now()) @map("created_at")
  updatedAt    DateTime         @updatedAt @map("updated_at")
  application  OauthApplication @relation(fields: [clientId], references: [clientId], onDelete: Cascade)
  user         User             @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([clientId, userId])
  @@index([userId])
  @@map("oauth_consents")
}

model Jwks {
  id         String    @id @default(uuid()) @db.Char(36)
  publicKey  String    @map("public_key") @db.Text
  privateKey String    @map("private_key") @db.Text
  createdAt  DateTime  @default(now()) @map("created_at")
  expiresAt  DateTime? @map("expires_at")

  @@map("jwks")
}

model IdCardBatch {
  id               String      @id @default(uuid()) @db.Char(36)
  institutionId    String      @map("institution_id") @db.Char(36)
  templateId       String      @map("template_id") @db.Char(36)
  totalRequested   Int         @map("total_requested")
  totalSucceeded   Int         @default(0) @map("total_succeeded")
  totalFailed      Int         @default(0) @map("total_failed")
  failedStudentIds String      @default("[]") @map("failed_student_ids") @db.VarChar(10000)
  pdfUrl           String?     @map("pdf_url") @db.Text
  status           String      @default("pending") @db.VarChar(50)
  processingTimeMs Int?        @map("processing_time_ms")
  createdAt        DateTime    @default(now()) @map("created_at")
  updatedAt        DateTime    @updatedAt @map("updated_at")
  institution      Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  template         Template    @relation(fields: [templateId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([institutionId, status])
  @@index([templateId])
  @@map("id_card_batches")
}

model Guardian {
  id                String                @id @default(uuid()) @db.Char(36)
  institutionId     String                @map("institution_id") @db.Char(36)
  firstName         String                @map("first_name") @db.VarChar(100)
  lastName          String?               @map("last_name") @db.VarChar(100)
  whatsappNumber    String                @map("whatsapp_number") @db.VarChar(20)
  role              GuardianRole          @default(other)
  preferredLanguage String?               @map("preferred_language") @db.VarChar(10)
  preferredDialect  String?               @map("preferred_dialect") @db.VarChar(50)
  preferredMedium   PreferredMedium?      @map("preferred_medium")
  marketingConsent  Boolean               @default(false) @map("marketing_consent")
  whatsappVerified  Boolean               @default(false) @map("whatsapp_verified")
  source            GuardianSource        @default(backfill)
  createdAt         DateTime              @default(now()) @map("created_at")
  updatedAt         DateTime              @updatedAt @map("updated_at")
  studentLinks      GuardianStudentLink[]
  institution       Institution           @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([institutionId, whatsappNumber])
  @@index([institutionId])
  @@index([whatsappNumber])
  @@map("guardians")
}

model GuardianStudentLink {
  id               String      @id @default(uuid()) @db.Char(36)
  institutionId    String      @map("institution_id") @db.Char(36)
  guardianId       String      @map("guardian_id") @db.Char(36)
  studentId        String      @map("student_id") @db.Char(36)
  isPrimary        Boolean     @default(false) @map("is_primary")
  notifyAttendance Boolean     @default(true) @map("notify_attendance")
  notifyFees       Boolean     @default(true) @map("notify_fees")
  notifyHomework   Boolean     @default(true) @map("notify_homework")
  notifyExams      Boolean     @default(true) @map("notify_exams")
  notifyTransport  Boolean     @default(false) @map("notify_transport")
  createdAt        DateTime    @default(now()) @map("created_at")
  updatedAt        DateTime    @updatedAt @map("updated_at")
  guardian         Guardian    @relation(fields: [guardianId], references: [id], onDelete: Cascade)
  institution      Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student          Student     @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@unique([guardianId, studentId])
  @@index([institutionId])
  @@index([guardianId])
  @@index([studentId])
  @@map("guardian_student_links")
}

model MessageTemplate {
  id               String           @id @default(uuid()) @db.Char(36)
  institutionId    String           @map("institution_id") @db.Char(36)
  code             String           @db.VarChar(100)
  metaTemplateName String?          @map("meta_template_name") @db.VarChar(255)
  category         TemplateCategory
  channel          TemplateChannel  @default(whatsapp)
  language         String           @db.VarChar(10)
  dialect          String?          @db.VarChar(50)
  bodyText         String           @map("body_text") @db.Text
  placeholders     Json?
  buttonConfig     Json?            @map("button_config")
  status           TemplateStatus   @default(draft)
  metaTemplateId   String?          @map("meta_template_id") @db.VarChar(100)
  createdAt        DateTime         @default(now()) @map("created_at")
  updatedAt        DateTime         @updatedAt @map("updated_at")
  institution      Institution      @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([institutionId, code, language])
  @@index([institutionId])
  @@index([code])
  @@map("message_templates")
}

model Outbox {
  id             String            @id @default(uuid()) @db.Char(36)
  institutionId  String            @map("institution_id") @db.Char(36)
  recipientType  RecipientType     @map("recipient_type")
  recipientId    String            @map("recipient_id") @db.Char(36)
  channel        OutboxChannel     @default(whatsapp)
  templateCode   String            @map("template_code") @db.VarChar(100)
  variables      Json?
  category       TemplateCategory?
  priority       OutboxPriority    @default(normal)
  idempotencyKey String            @unique @map("idempotency_key") @db.VarChar(255)
  status         OutboxStatus      @default(pending)
  windowUsed     Boolean           @default(false) @map("window_used")
  sentAt         DateTime?         @map("sent_at")
  attempts       Int               @default(0)
  lastError      String?           @map("last_error") @db.Text
  createdAt      DateTime          @default(now()) @map("created_at")
  updatedAt      DateTime          @updatedAt @map("updated_at")
  digestItems    DigestQueue[]
  messages       Message[]
  institution    Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([status])
  @@index([recipientType, recipientId])
  @@map("outbox")
}

model Message {
  id            String            @id @default(uuid()) @db.Char(36)
  institutionId String            @map("institution_id") @db.Char(36)
  outboxId      String?           @map("outbox_id") @db.Char(36)
  guardianId    String?           @map("guardian_id") @db.Char(36)
  staffId       String?           @map("staff_id") @db.Char(36)
  channel       OutboxChannel
  templateCode  String?           @map("template_code") @db.VarChar(100)
  waMessageId   String?           @map("wa_message_id") @db.VarChar(255)
  direction     MessageDirection
  category      TemplateCategory?
  status        String            @db.VarChar(50)
  deliveredAt   DateTime?         @map("delivered_at")
  readAt        DateTime?         @map("read_at")
  failedAt      DateTime?         @map("failed_at")
  failureReason String?           @map("failure_reason") @db.Text
  createdAt     DateTime          @default(now()) @map("created_at")
  updatedAt     DateTime          @updatedAt @map("updated_at")
  institution   Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  outbox        Outbox?           @relation(fields: [outboxId], references: [id])

  @@index([institutionId])
  @@index([guardianId])
  @@index([waMessageId])
  @@index([outboxId])
  @@map("messages")
}

model DigestQueue {
  id            String      @id @default(uuid()) @db.Char(36)
  institutionId String      @map("institution_id") @db.Char(36)
  guardianId    String      @map("guardian_id") @db.Char(36)
  eventType     String      @map("event_type") @db.VarChar(100)
  eventPayload  Json        @map("event_payload")
  scheduledFor  DateTime    @map("scheduled_for")
  sentAt        DateTime?   @map("sent_at")
  outboxId      String?     @map("outbox_id") @db.Char(36)
  createdAt     DateTime    @default(now()) @map("created_at")
  institution   Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  outbox        Outbox?     @relation(fields: [outboxId], references: [id])

  @@index([institutionId])
  @@index([guardianId])
  @@index([outboxId], map: "digest_queue_outbox_id_fkey")
  @@map("digest_queue")
}

model FeeStructure {
  id               String       @id @default(uuid()) @db.Char(36)
  institutionId    String       @map("institution_id") @db.Char(36)
  academicYear     String       @map("academic_year") @db.VarChar(20)
  classId          String?      @map("class_id") @db.Char(36)
  name             String       @db.VarChar(255)
  category         FeeCategory
  amount           Decimal      @db.Decimal(10, 2)
  frequency        FeeFrequency
  dueDayOfMonth    Int?         @map("due_day_of_month")
  lateFeeAmount    Decimal?     @map("late_fee_amount") @db.Decimal(10, 2)
  lateFeeAfterDays Int?         @map("late_fee_after_days")
  isActive         Boolean      @default(true) @map("is_active")
  createdAt        DateTime     @default(now()) @map("created_at")
  updatedAt        DateTime     @updatedAt @map("updated_at")
  invoices         FeeInvoice[]
  institution      Institution  @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([classId])
  @@map("fee_structures")
}

model FeeInvoice {
  id             String            @id @default(uuid()) @db.Char(36)
  institutionId  String            @map("institution_id") @db.Char(36)
  studentId      String            @map("student_id") @db.Char(36)
  feeStructureId String?           @map("fee_structure_id") @db.Char(36)
  invoiceNumber  String            @map("invoice_number") @db.VarChar(100)
  amount         Decimal           @db.Decimal(10, 2)
  discount       Decimal           @default(0.00) @db.Decimal(10, 2)
  lateFee        Decimal           @default(0.00) @map("late_fee") @db.Decimal(10, 2)
  netAmount      Decimal           @map("net_amount") @db.Decimal(10, 2)
  dueDate        DateTime          @map("due_date") @db.Date
  status         InvoiceStatus     @default(unpaid)
  paidAmount     Decimal           @default(0.00) @map("paid_amount") @db.Decimal(10, 2)
  paidAt         DateTime?         @map("paid_at")
  notes          String?           @db.Text
  paymentLinkUrl String?           @map("payment_link_url") @db.VarChar(500)
  gatewayOrderId String?           @map("gateway_order_id") @db.VarChar(255)
  createdAt      DateTime          @default(now()) @map("created_at")
  updatedAt      DateTime          @updatedAt @map("updated_at")
  feeStructure   FeeStructure?     @relation(fields: [feeStructureId], references: [id])
  institution    Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  student        Student           @relation(fields: [studentId], references: [id], onDelete: Cascade)
  paymentClaims  FeePaymentClaim[]
  payments       FeePayment[]

  @@unique([institutionId, invoiceNumber])
  @@index([institutionId])
  @@index([studentId])
  @@index([status])
  @@index([gatewayOrderId])
  @@index([feeStructureId], map: "fee_invoices_fee_structure_id_fkey")
  @@map("fee_invoices")
}

model FeePayment {
  id               String           @id @default(uuid()) @db.Char(36)
  institutionId    String           @map("institution_id") @db.Char(36)
  invoiceId        String           @map("invoice_id") @db.Char(36)
  amount           Decimal          @db.Decimal(10, 2)
  paidByGuardianId String?          @map("paid_by_guardian_id") @db.Char(36)
  method           PaymentMethod
  gatewayProvider  GatewayProvider? @map("gateway_provider")
  gatewayPaymentId String?          @map("gateway_payment_id") @db.VarChar(255)
  gatewayOrderId   String?          @map("gateway_order_id") @db.VarChar(255)
  status           PaymentStatus    @default(initiated)
  paidAt           DateTime?        @map("paid_at")
  receiptUrl       String?          @map("receipt_url") @db.VarChar(500)
  createdAt        DateTime         @default(now()) @map("created_at")
  updatedAt        DateTime         @updatedAt @map("updated_at")
  institution      Institution      @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  invoice          FeeInvoice       @relation(fields: [invoiceId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([invoiceId])
  @@index([gatewayPaymentId])
  @@map("fee_payments")
}

model InboundMedia {
  id             String             @id @default(uuid()) @db.Char(36)
  institutionId  String             @map("institution_id") @db.Char(36)
  guardianId     String             @map("guardian_id") @db.Char(36)
  waMediaId      String             @unique @map("wa_media_id") @db.VarChar(255)
  objectPath     String?            @map("object_path") @db.VarChar(500)
  mediaType      MediaType          @map("media_type")
  mimeType       String?            @map("mime_type") @db.VarChar(100)
  fileSizeBytes  Int?               @map("file_size_bytes")
  intentDetected String?            @map("intent_detected") @db.VarChar(100)
  actionTaken    String?            @map("action_taken") @db.VarChar(255)
  visionUsed     Boolean            @default(false) @map("vision_used")
  status         InboundMediaStatus @default(received)
  transcript     String?            @db.Text
  extractedText  String?            @map("extracted_text") @db.Text
  metadata       Json?
  errorMessage   String?            @map("error_message") @db.Text
  processedAt    DateTime?          @map("processed_at")
  createdAt      DateTime           @default(now()) @map("created_at")
  updatedAt      DateTime           @updatedAt @map("updated_at")
  institution    Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([guardianId])
  @@map("inbound_media")
}

model Conversation {
  id                     String                @id @default(uuid()) @db.Char(36)
  institutionId          String                @map("institution_id") @db.Char(36)
  guardianId             String                @map("guardian_id") @db.Char(36)
  studentId              String?               @map("student_id") @db.Char(36)
  lastIntent             String?               @map("last_intent") @db.VarChar(100)
  lastMessageAt          DateTime?             @map("last_message_at")
  messageCount           Int                   @default(0) @map("message_count")
  context                Json?
  serviceWindowExpiresAt DateTime?             @map("service_window_expires_at")
  open                   Boolean               @default(true)
  createdAt              DateTime              @default(now()) @map("created_at")
  updatedAt              DateTime              @updatedAt @map("updated_at")
  messages               ConversationMessage[]
  institution            Institution           @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([institutionId, guardianId])
  @@index([institutionId])
  @@index([guardianId])
  @@map("conversations")
}

model ConversationMessage {
  id             String           @id @default(uuid()) @db.Char(36)
  institutionId  String           @map("institution_id") @db.Char(36)
  conversationId String           @map("conversation_id") @db.Char(36)
  direction      MessageDirection
  text           String           @db.VarChar(2000)
  intent         String?          @db.VarChar(100)
  createdAt      DateTime         @default(now()) @map("created_at")
  conversation   Conversation     @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  institution    Institution      @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([conversationId])
  @@map("conversation_messages")
}

model FeePaymentClaim {
  id                    String         @id @default(uuid()) @db.Char(36)
  institutionId         String         @map("institution_id") @db.Char(36)
  invoiceId             String         @map("invoice_id") @db.Char(36)
  submittedByGuardianId String         @map("submitted_by_guardian_id") @db.Char(36)
  objectPath            String         @map("object_path") @db.VarChar(500)
  mediaType             MediaType      @map("media_type")
  claimAmount           Decimal?       @map("claim_amount") @db.Decimal(10, 2)
  paymentMethodClaimed  PaymentMethod? @map("payment_method_claimed")
  status                FeeClaimStatus @default(pending_review)
  reviewedBy            String?        @map("reviewed_by") @db.Char(36)
  reviewedAt            DateTime?      @map("reviewed_at")
  rejectionReason       String?        @map("rejection_reason") @db.Text
  createdAt             DateTime       @default(now()) @map("created_at")
  updatedAt             DateTime       @updatedAt @map("updated_at")
  institution           Institution    @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  invoice               FeeInvoice     @relation(fields: [invoiceId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([invoiceId])
  @@index([status])
  @@map("fee_payment_claims")
}

model TimetablePeriod {
  id            String          @id @default(uuid()) @db.Char(36)
  institutionId String          @map("institution_id") @db.Char(36)
  name          String          @db.VarChar(100)
  startTime     String          @map("start_time") @db.VarChar(5)
  endTime       String          @map("end_time") @db.VarChar(5)
  sequence      Int             @default(0)
  isBreak       Boolean         @default(false) @map("is_break")
  createdAt     DateTime        @default(now()) @map("created_at")
  institution   Institution     @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  slots         TimetableSlot[]

  @@unique([institutionId, name])
  @@index([institutionId])
  @@map("timetable_periods")
}

model TimetableSlot {
  id            String          @id @default(uuid()) @db.Char(36)
  institutionId String          @map("institution_id") @db.Char(36)
  sectionId     String          @map("section_id") @db.Char(36)
  dayOfWeek     DayOfWeek       @map("day_of_week")
  periodId      String          @map("period_id") @db.Char(36)
  subjectName   String          @map("subject_name") @db.VarChar(255)
  subjectId     String?         @map("subject_id") @db.Char(36)
  teacherId     String?         @map("teacher_id") @db.Char(36)
  room          String?         @db.VarChar(100)
  createdAt     DateTime        @default(now()) @map("created_at")
  updatedAt     DateTime        @updatedAt @map("updated_at")
  substitutions Substitution[]
  period        TimetablePeriod @relation(fields: [periodId], references: [id], onDelete: Cascade)

  @@unique([sectionId, dayOfWeek, periodId])
  @@index([institutionId])
  @@index([sectionId])
  @@index([teacherId])
  @@index([periodId], map: "timetable_slots_period_id_fkey")
  @@map("timetable_slots")
}

model Substitution {
  id                  String             @id @default(uuid()) @db.Char(36)
  institutionId       String             @map("institution_id") @db.Char(36)
  slotId              String             @map("slot_id") @db.Char(36)
  date                DateTime           @db.Date
  originalTeacherId   String?            @map("original_teacher_id") @db.Char(36)
  substituteTeacherId String             @map("substitute_teacher_id") @db.Char(36)
  reason              String?            @db.Text
  status              SubstitutionStatus @default(planned)
  createdAt           DateTime           @default(now()) @map("created_at")
  updatedAt           DateTime           @updatedAt @map("updated_at")
  slot                TimetableSlot      @relation(fields: [slotId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([date])
  @@index([substituteTeacherId])
  @@index([slotId], map: "substitutions_slot_id_fkey")
  @@map("substitutions")
}

model LedgerAccount {
  id            String            @id @default(uuid()) @db.Char(36)
  institutionId String            @map("institution_id") @db.Char(36)
  code          String            @db.VarChar(50)
  name          String            @db.VarChar(255)
  type          LedgerAccountType
  isSystem      Boolean           @default(false) @map("is_system")
  isActive      Boolean           @default(true) @map("is_active")
  createdAt     DateTime          @default(now()) @map("created_at")
  updatedAt     DateTime          @updatedAt @map("updated_at")
  institution   Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([institutionId, code])
  @@index([institutionId])
  @@index([type])
  @@map("ledger_accounts")
}

model JournalEntry {
  id              String        @id @default(uuid()) @db.Char(36)
  institutionId   String        @map("institution_id") @db.Char(36)
  voucherNumber   String        @map("voucher_number") @db.VarChar(50)
  entryDate       DateTime      @map("entry_date") @db.Date
  type            VoucherType   @default(journal)
  narration       String?       @db.Text
  totalAmount     Decimal       @map("total_amount") @db.Decimal(14, 2)
  referenceType   String?       @map("reference_type") @db.VarChar(50)
  referenceId     String?       @map("reference_id") @db.Char(36)
  status          JournalStatus @default(posted)
  createdByUserId String?       @map("created_by_user_id") @db.Char(36)
  createdAt       DateTime      @default(now()) @map("created_at")
  institution     Institution   @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  lines           JournalLine[]

  @@unique([institutionId, voucherNumber])
  @@index([institutionId])
  @@index([entryDate])
  @@map("journal_entries")
}

model JournalLine {
  id            String       @id @default(uuid()) @db.Char(36)
  institutionId String       @map("institution_id") @db.Char(36)
  entryId       String       @map("entry_id") @db.Char(36)
  accountId     String       @map("account_id") @db.Char(36)
  debit         Decimal      @default(0.00) @db.Decimal(14, 2)
  credit        Decimal      @default(0.00) @db.Decimal(14, 2)
  createdAt     DateTime     @default(now()) @map("created_at")
  entry         JournalEntry @relation(fields: [entryId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([entryId])
  @@index([accountId])
  @@map("journal_lines")
}

model StaffMember {
  id               String            @id @default(uuid()) @db.Char(36)
  institutionId    String            @map("institution_id") @db.Char(36)
  employeeCode     String            @map("employee_code") @db.VarChar(50)
  firstName        String            @map("first_name") @db.VarChar(255)
  lastName         String?           @map("last_name") @db.VarChar(255)
  designation      String?           @db.VarChar(255)
  department       String?           @db.VarChar(255)
  employmentType   EmploymentType    @default(full_time) @map("employment_type")
  dateOfJoining    DateTime?         @map("date_of_joining") @db.Date
  email            String?           @db.VarChar(255)
  phone            String?           @db.VarChar(20)
  status           StaffStatus       @default(active)
  userId           String?           @map("user_id") @db.Char(36)
  createdAt        DateTime          @default(now()) @map("created_at")
  updatedAt        DateTime          @updatedAt @map("updated_at")
  leaveRequests    LeaveRequest[]
  payslips         Payslip[]
  salaryStructures SalaryStructure[]
  institution      Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([institutionId, employeeCode])
  @@index([institutionId])
  @@index([status])
  @@map("staff_members")
}

model SalaryStructure {
  id              String      @id @default(uuid()) @db.Char(36)
  institutionId   String      @map("institution_id") @db.Char(36)
  staffId         String      @map("staff_id") @db.Char(36)
  effectiveFrom   DateTime    @map("effective_from") @db.Date
  basic           Decimal     @db.Decimal(12, 2)
  hra             Decimal     @default(0.00) @db.Decimal(12, 2)
  conveyance      Decimal     @default(0.00) @db.Decimal(12, 2)
  special         Decimal     @default(0.00) @db.Decimal(12, 2)
  otherAllowances Json?       @map("other_allowances")
  pfEnabled       Boolean     @default(true) @map("pf_enabled")
  esiEnabled      Boolean     @default(false) @map("esi_enabled")
  professionalTax Decimal     @default(0.00) @map("professional_tax") @db.Decimal(12, 2)
  tds             Decimal     @default(0.00) @db.Decimal(12, 2)
  otherDeductions Json?       @map("other_deductions")
  isActive        Boolean     @default(true) @map("is_active")
  createdAt       DateTime    @default(now()) @map("created_at")
  staff           StaffMember @relation(fields: [staffId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([staffId])
  @@map("salary_structures")
}

model Payslip {
  id              String        @id @default(uuid()) @db.Char(36)
  institutionId   String        @map("institution_id") @db.Char(36)
  staffId         String        @map("staff_id") @db.Char(36)
  month           Int
  year            Int
  grossEarnings   Decimal       @map("gross_earnings") @db.Decimal(12, 2)
  totalDeductions Decimal       @map("total_deductions") @db.Decimal(12, 2)
  netPay          Decimal       @map("net_pay") @db.Decimal(12, 2)
  breakdown       Json
  status          PayslipStatus @default(draft)
  pdfUrl          String?       @map("pdf_url") @db.VarChar(500)
  paidAt          DateTime?     @map("paid_at")
  createdAt       DateTime      @default(now()) @map("created_at")
  updatedAt       DateTime      @updatedAt @map("updated_at")
  staff           StaffMember   @relation(fields: [staffId], references: [id], onDelete: Cascade)

  @@unique([staffId, month, year])
  @@index([institutionId])
  @@index([staffId])
  @@map("payslips")
}

model LeaveRequest {
  id               String      @id @default(uuid()) @db.Char(36)
  institutionId    String      @map("institution_id") @db.Char(36)
  staffId          String      @map("staff_id") @db.Char(36)
  type             LeaveType   @default(casual)
  fromDate         DateTime    @map("from_date") @db.Date
  toDate           DateTime    @map("to_date") @db.Date
  days             Int
  reason           String?     @db.Text
  status           LeaveStatus @default(pending)
  reviewedByUserId String?     @map("reviewed_by_user_id") @db.Char(36)
  reviewedAt       DateTime?   @map("reviewed_at")
  createdAt        DateTime    @default(now()) @map("created_at")
  updatedAt        DateTime    @updatedAt @map("updated_at")
  staff            StaffMember @relation(fields: [staffId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([staffId])
  @@index([status])
  @@map("leave_requests")
}

model TransportRoute {
  id            String             @id @default(uuid()) @db.Char(36)
  institutionId String             @map("institution_id") @db.Char(36)
  name          String             @db.VarChar(255)
  code          String             @db.VarChar(50)
  description   String?            @db.Text
  vehicleNumber String?            @map("vehicle_number") @db.VarChar(50)
  driverName    String?            @map("driver_name") @db.VarChar(255)
  driverPhone   String?            @map("driver_phone") @db.VarChar(20)
  capacity      Int?
  feeAmount     Decimal?           @map("fee_amount") @db.Decimal(10, 2)
  isActive      Boolean            @default(true) @map("is_active")
  createdAt     DateTime           @default(now()) @map("created_at")
  updatedAt     DateTime           @updatedAt @map("updated_at")
  assignments   StudentTransport[]
  institution   Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  stops         TransportStop[]
  trips         TransportTrip[]

  @@unique([institutionId, code])
  @@index([institutionId])
  @@map("transport_routes")
}

model TransportStop {
  id            String         @id @default(uuid()) @db.Char(36)
  institutionId String         @map("institution_id") @db.Char(36)
  routeId       String         @map("route_id") @db.Char(36)
  name          String         @db.VarChar(255)
  sequence      Int            @default(0)
  pickupTime    String?        @map("pickup_time") @db.VarChar(5)
  dropTime      String?        @map("drop_time") @db.VarChar(5)
  latitude      Float?
  longitude     Float?
  createdAt     DateTime       @default(now()) @map("created_at")
  route         TransportRoute @relation(fields: [routeId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([routeId])
  @@map("transport_stops")
}

model StudentTransport {
  id            String                  @id @default(uuid()) @db.Char(36)
  institutionId String                  @map("institution_id") @db.Char(36)
  studentId     String                  @map("student_id") @db.Char(36)
  routeId       String                  @map("route_id") @db.Char(36)
  stopId        String?                 @map("stop_id") @db.Char(36)
  type          TransportAssignmentType @default(both)
  isActive      Boolean                 @default(true) @map("is_active")
  createdAt     DateTime                @default(now()) @map("created_at")
  updatedAt     DateTime                @updatedAt @map("updated_at")
  route         TransportRoute          @relation(fields: [routeId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([studentId])
  @@index([routeId])
  @@map("student_transport")
}

model TransportTrip {
  id            String                  @id @default(uuid()) @db.Char(36)
  institutionId String                  @map("institution_id") @db.Char(36)
  routeId       String                  @map("route_id") @db.Char(36)
  tripDate      DateTime                @map("trip_date") @db.Date
  direction     TransportAssignmentType @default(pickup)
  status        TransportTripStatus     @default(scheduled)
  startedAt     DateTime?               @map("started_at")
  completedAt   DateTime?               @map("completed_at")
  lastLatitude  Float?                  @map("last_latitude")
  lastLongitude Float?                  @map("last_longitude")
  lastPingAt    DateTime?               @map("last_ping_at")
  createdAt     DateTime                @default(now()) @map("created_at")
  updatedAt     DateTime                @updatedAt @map("updated_at")
  route         TransportRoute          @relation(fields: [routeId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([routeId])
  @@map("transport_trips")
}

model Enquiry {
  id                 String            @id @default(uuid()) @db.Char(36)
  institutionId      String            @map("institution_id") @db.Char(36)
  enquiryNumber      String            @map("enquiry_number") @db.VarChar(50)
  studentName        String            @map("student_name") @db.VarChar(255)
  guardianName       String?           @map("guardian_name") @db.VarChar(255)
  phone              String            @db.VarChar(20)
  email              String?           @db.VarChar(255)
  classInterested    String?           @map("class_interested") @db.VarChar(255)
  classId            String?           @map("class_id") @db.Char(36)
  source             EnquirySource     @default(other)
  status             EnquiryStatus     @default(new)
  assignedToUserId   String?           @map("assigned_to_user_id") @db.Char(36)
  followUpAt         DateTime?         @map("follow_up_at")
  notes              String?           @db.Text
  convertedStudentId String?           @map("converted_student_id") @db.Char(36)
  createdAt          DateTime          @default(now()) @map("created_at")
  updatedAt          DateTime          @updatedAt @map("updated_at")
  institution        Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  activities         EnquiryActivity[]

  @@unique([institutionId, enquiryNumber])
  @@index([institutionId])
  @@index([status])
  @@index([phone])
  @@index([assignedToUserId])
  @@map("enquiries")
}

model EnquiryActivity {
  id              String              @id @default(uuid()) @db.Char(36)
  institutionId   String              @map("institution_id") @db.Char(36)
  enquiryId       String              @map("enquiry_id") @db.Char(36)
  type            EnquiryActivityType @default(note)
  description     String              @db.Text
  createdByUserId String?             @map("created_by_user_id") @db.Char(36)
  createdAt       DateTime            @default(now()) @map("created_at")
  enquiry         Enquiry             @relation(fields: [enquiryId], references: [id], onDelete: Cascade)
  institution     Institution         @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([enquiryId])
  @@map("enquiry_activities")
}

model HostelBlock {
  id            String       @id @default(uuid()) @db.Char(36)
  institutionId String       @map("institution_id") @db.Char(36)
  name          String       @db.VarChar(255)
  code          String       @db.VarChar(50)
  type          HostelType   @default(boys)
  wardenName    String?      @map("warden_name") @db.VarChar(255)
  wardenPhone   String?      @map("warden_phone") @db.VarChar(20)
  totalRooms    Int          @default(0) @map("total_rooms")
  isActive      Boolean      @default(true) @map("is_active")
  createdAt     DateTime     @default(now()) @map("created_at")
  updatedAt     DateTime     @updatedAt @map("updated_at")
  institution   Institution  @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  rooms         HostelRoom[]

  @@unique([institutionId, code])
  @@index([institutionId])
  @@map("hostel_blocks")
}

model HostelRoom {
  id            String            @id @default(uuid()) @db.Char(36)
  institutionId String            @map("institution_id") @db.Char(36)
  blockId       String            @map("block_id") @db.Char(36)
  roomNumber    String            @map("room_number") @db.VarChar(50)
  floor         Int               @default(0)
  capacity      Int               @default(1)
  occupied      Int               @default(0)
  roomType      String?           @map("room_type") @db.VarChar(50)
  monthlyRent   Decimal?          @map("monthly_rent") @db.Decimal(10, 2)
  isActive      Boolean           @default(true) @map("is_active")
  createdAt     DateTime          @default(now()) @map("created_at")
  updatedAt     DateTime          @updatedAt @map("updated_at")
  allotments    HostelAllotment[]
  block         HostelBlock       @relation(fields: [blockId], references: [id], onDelete: Cascade)
  institution   Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([blockId, roomNumber])
  @@index([institutionId])
  @@index([blockId])
  @@map("hostel_rooms")
}

model HostelAllotment {
  id            String          @id @default(uuid()) @db.Char(36)
  institutionId String          @map("institution_id") @db.Char(36)
  roomId        String          @map("room_id") @db.Char(36)
  studentId     String          @map("student_id") @db.Char(36)
  bedNumber     String?         @map("bed_number") @db.VarChar(20)
  status        AllotmentStatus @default(active)
  allottedAt    DateTime        @default(now()) @map("allotted_at")
  vacatedAt     DateTime?       @map("vacated_at")
  createdAt     DateTime        @default(now()) @map("created_at")
  updatedAt     DateTime        @updatedAt @map("updated_at")
  institution   Institution     @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  room          HostelRoom      @relation(fields: [roomId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([roomId])
  @@index([studentId])
  @@map("hostel_allotments")
}

model MessBill {
  id            String         @id @default(uuid()) @db.Char(36)
  institutionId String         @map("institution_id") @db.Char(36)
  studentId     String         @map("student_id") @db.Char(36)
  billMonth     String         @map("bill_month") @db.VarChar(7)
  amount        Decimal        @db.Decimal(10, 2)
  status        MessBillStatus @default(pending)
  dueDate       DateTime?      @map("due_date") @db.Date
  paidAt        DateTime?      @map("paid_at")
  createdAt     DateTime       @default(now()) @map("created_at")
  updatedAt     DateTime       @updatedAt @map("updated_at")
  institution   Institution    @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([studentId, billMonth])
  @@index([institutionId])
  @@index([status])
  @@map("mess_bills")
}

model InventoryCategory {
  id            String          @id @default(uuid()) @db.Char(36)
  institutionId String          @map("institution_id") @db.Char(36)
  name          String          @db.VarChar(255)
  type          InventoryType   @default(consumable)
  createdAt     DateTime        @default(now()) @map("created_at")
  updatedAt     DateTime        @updatedAt @map("updated_at")
  institution   Institution     @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  items         InventoryItem[]

  @@unique([institutionId, name])
  @@index([institutionId])
  @@map("inventory_categories")
}

model InventoryItem {
  id            String             @id @default(uuid()) @db.Char(36)
  institutionId String             @map("institution_id") @db.Char(36)
  categoryId    String             @map("category_id") @db.Char(36)
  name          String             @db.VarChar(255)
  sku           String?            @db.VarChar(100)
  unit          String             @default("pcs") @db.VarChar(50)
  quantity      Int                @default(0)
  reorderLevel  Int                @default(0) @map("reorder_level")
  unitCost      Decimal?           @map("unit_cost") @db.Decimal(10, 2)
  location      String?            @db.VarChar(255)
  isActive      Boolean            @default(true) @map("is_active")
  createdAt     DateTime           @default(now()) @map("created_at")
  updatedAt     DateTime           @updatedAt @map("updated_at")
  category      InventoryCategory  @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  institution   Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  transactions  StockTransaction[]

  @@index([institutionId])
  @@index([categoryId])
  @@map("inventory_items")
}

model StockTransaction {
  id            String        @id @default(uuid()) @db.Char(36)
  institutionId String        @map("institution_id") @db.Char(36)
  itemId        String        @map("item_id") @db.Char(36)
  type          StockTxnType
  quantity      Int
  balanceAfter  Int           @map("balance_after")
  reason        String?       @db.VarChar(500)
  performedBy   String?       @map("performed_by") @db.Char(36)
  createdAt     DateTime      @default(now()) @map("created_at")
  institution   Institution   @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  item          InventoryItem @relation(fields: [itemId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([itemId])
  @@index([type])
  @@map("stock_transactions")
}

model HealthRecord {
  id            String      @id @default(uuid()) @db.Char(36)
  institutionId String      @map("institution_id") @db.Char(36)
  studentId     String      @map("student_id") @db.Char(36)
  bloodGroup    String?     @map("blood_group") @db.VarChar(10)
  allergies     String?     @db.Text
  conditions    String?     @db.Text
  heightCm      Decimal?    @map("height_cm") @db.Decimal(5, 2)
  weightKg      Decimal?    @map("weight_kg") @db.Decimal(5, 2)
  lastCheckup   DateTime?   @map("last_checkup") @db.Date
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")
  institution   Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([institutionId, studentId])
  @@index([institutionId])
  @@map("health_records")
}

model ClinicVisit {
  id               String      @id @default(uuid()) @db.Char(36)
  institutionId    String      @map("institution_id") @db.Char(36)
  studentId        String      @map("student_id") @db.Char(36)
  visitDate        DateTime    @default(now()) @map("visit_date")
  symptoms         String?     @db.Text
  diagnosis        String?     @db.Text
  treatment        String?     @db.Text
  attendedBy       String?     @map("attended_by") @db.VarChar(255)
  guardianNotified Boolean     @default(false) @map("guardian_notified")
  createdAt        DateTime    @default(now()) @map("created_at")
  updatedAt        DateTime    @updatedAt @map("updated_at")
  institution      Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([studentId])
  @@map("clinic_visits")
}

model VaccinationRecord {
  id               String      @id @default(uuid()) @db.Char(36)
  institutionId    String      @map("institution_id") @db.Char(36)
  studentId        String      @map("student_id") @db.Char(36)
  vaccineName      String      @map("vaccine_name") @db.VarChar(255)
  dateAdministered DateTime?   @map("date_administered") @db.Date
  nextDue          DateTime?   @map("next_due") @db.Date
  notes            String?     @db.Text
  createdAt        DateTime    @default(now()) @map("created_at")
  updatedAt        DateTime    @updatedAt @map("updated_at")
  institution      Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([studentId])
  @@map("vaccination_records")
}

model VisitorLog {
  id            String        @id @default(uuid()) @db.Char(36)
  institutionId String        @map("institution_id") @db.Char(36)
  visitorName   String        @map("visitor_name") @db.VarChar(255)
  phone         String?       @db.VarChar(20)
  purpose       String?       @db.VarChar(500)
  whomToMeet    String?       @map("whom_to_meet") @db.VarChar(255)
  badgeNumber   String?       @map("badge_number") @db.VarChar(50)
  status        VisitorStatus @default(checked_in)
  checkInAt     DateTime      @default(now()) @map("check_in_at")
  checkOutAt    DateTime?     @map("check_out_at")
  createdAt     DateTime      @default(now()) @map("created_at")
  updatedAt     DateTime      @updatedAt @map("updated_at")
  institution   Institution   @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([status])
  @@map("visitor_logs")
}

model GatePass {
  id            String       @id @default(uuid()) @db.Char(36)
  institutionId String       @map("institution_id") @db.Char(36)
  studentId     String       @map("student_id") @db.Char(36)
  type          GatePassType @default(early_leave)
  reason        String?      @db.Text
  approvedBy    String?      @map("approved_by") @db.VarChar(255)
  issuedAt      DateTime     @default(now()) @map("issued_at")
  validUntil    DateTime?    @map("valid_until")
  createdAt     DateTime     @default(now()) @map("created_at")
  updatedAt     DateTime     @updatedAt @map("updated_at")
  institution   Institution  @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([studentId])
  @@map("gate_passes")
}

model CceAssessment {
  id            String      @id @default(uuid()) @db.Char(36)
  institutionId String      @map("institution_id") @db.Char(36)
  sectionId     String      @map("section_id") @db.Char(36)
  subjectName   String      @map("subject_name") @db.VarChar(255)
  name          String      @db.VarChar(255)
  termType      CceTermType @map("term_type")
  maxMarks      Decimal     @default(100.00) @map("max_marks") @db.Decimal(6, 2)
  weightage     Int         @default(100)
  conductedOn   DateTime?   @map("conducted_on") @db.Date
  status        CceStatus   @default(open)
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")
  institution   Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  marks         CceMark[]

  @@index([institutionId])
  @@index([sectionId])
  @@index([termType])
  @@map("cce_assessments")
}

model CceMark {
  id            String        @id @default(uuid()) @db.Char(36)
  institutionId String        @map("institution_id") @db.Char(36)
  assessmentId  String        @map("assessment_id") @db.Char(36)
  studentId     String        @map("student_id") @db.Char(36)
  marksObtained Decimal       @map("marks_obtained") @db.Decimal(6, 2)
  grade         String?       @db.VarChar(4)
  remarks       String?       @db.Text
  createdAt     DateTime      @default(now()) @map("created_at")
  updatedAt     DateTime      @updatedAt @map("updated_at")
  assessment    CceAssessment @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
  institution   Institution   @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([assessmentId, studentId])
  @@index([institutionId])
  @@index([studentId])
  @@map("cce_marks")
}

model Assignment {
  id            String                 @id @default(uuid()) @db.Char(36)
  institutionId String                 @map("institution_id") @db.Char(36)
  sectionId     String                 @map("section_id") @db.Char(36)
  subjectName   String                 @map("subject_name") @db.VarChar(255)
  title         String                 @db.VarChar(255)
  description   String?                @db.Text
  assignedBy    String?                @map("assigned_by") @db.Char(36)
  dueDate       DateTime?              @map("due_date")
  maxMarks      Decimal?               @map("max_marks") @db.Decimal(6, 2)
  attachmentUrl String?                @map("attachment_url") @db.VarChar(500)
  status        AssignmentStatus       @default(draft)
  createdAt     DateTime               @default(now()) @map("created_at")
  updatedAt     DateTime               @updatedAt @map("updated_at")
  submissions   AssignmentSubmission[]
  institution   Institution            @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([sectionId])
  @@index([status])
  @@map("assignments")
}

model AssignmentSubmission {
  id            String           @id @default(uuid()) @db.Char(36)
  institutionId String           @map("institution_id") @db.Char(36)
  assignmentId  String           @map("assignment_id") @db.Char(36)
  studentId     String           @map("student_id") @db.Char(36)
  content       String?          @db.Text
  attachmentUrl String?          @map("attachment_url") @db.VarChar(500)
  marksObtained Decimal?         @map("marks_obtained") @db.Decimal(6, 2)
  feedback      String?          @db.Text
  status        SubmissionStatus @default(submitted)
  submittedAt   DateTime         @default(now()) @map("submitted_at")
  gradedAt      DateTime?        @map("graded_at")
  createdAt     DateTime         @default(now()) @map("created_at")
  updatedAt     DateTime         @updatedAt @map("updated_at")
  assignment    Assignment       @relation(fields: [assignmentId], references: [id], onDelete: Cascade)
  institution   Institution      @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([assignmentId, studentId])
  @@index([institutionId])
  @@index([studentId])
  @@map("assignment_submissions")
}

model Notice {
  id            String         @id @default(uuid()) @db.Char(36)
  institutionId String         @map("institution_id") @db.Char(36)
  title         String         @db.VarChar(255)
  body          String         @db.Text
  audience      NoticeAudience @default(all)
  category      NoticeCategory @default(circular)
  isPinned      Boolean        @default(false) @map("is_pinned")
  status        NoticeStatus   @default(published)
  publishedAt   DateTime       @default(now()) @map("published_at")
  expiresAt     DateTime?      @map("expires_at")
  createdAt     DateTime       @default(now()) @map("created_at")
  updatedAt     DateTime       @updatedAt @map("updated_at")
  institution   Institution    @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([status])
  @@index([audience])
  @@map("notices")
}

model CalendarEvent {
  id            String      @id @default(uuid()) @db.Char(36)
  institutionId String      @map("institution_id") @db.Char(36)
  title         String      @db.VarChar(255)
  description   String?     @db.Text
  eventType     EventType   @default(event) @map("event_type")
  eventDate     DateTime    @map("event_date") @db.Date
  endDate       DateTime?   @map("end_date") @db.Date
  allDay        Boolean     @default(true) @map("all_day")
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")
  institution   Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([eventDate])
  @@map("calendar_events")
}

model SavedReport {
  id            String      @id @default(uuid()) @db.Char(36)
  institutionId String      @map("institution_id") @db.Char(36)
  name          String      @db.VarChar(255)
  reportType    String      @map("report_type") @db.VarChar(100)
  config        Json?
  createdBy     String?     @map("created_by") @db.Char(36)
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")
  institution   Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@map("saved_reports")
}

model Alumni {
  id                  String      @id @default(uuid()) @db.Char(36)
  institutionId       String      @map("institution_id") @db.Char(36)
  name                String      @db.VarChar(255)
  email               String?     @db.VarChar(255)
  phone               String?     @db.VarChar(20)
  graduationYear      Int?        @map("graduation_year")
  currentOrganization String?     @map("current_organization") @db.VarChar(255)
  designation         String?     @db.VarChar(255)
  location            String?     @db.VarChar(255)
  linkedinUrl         String?     @map("linkedin_url") @db.VarChar(500)
  willingToMentor     Boolean     @default(false) @map("willing_to_mentor")
  createdAt           DateTime    @default(now()) @map("created_at")
  updatedAt           DateTime    @updatedAt @map("updated_at")
  institution         Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([graduationYear])
  @@map("alumni")
}

model AlumniEvent {
  id            String      @id @default(uuid()) @db.Char(36)
  institutionId String      @map("institution_id") @db.Char(36)
  title         String      @db.VarChar(255)
  description   String?     @db.Text
  eventDate     DateTime    @map("event_date") @db.Date
  venue         String?     @db.VarChar(255)
  rsvpCount     Int         @default(0) @map("rsvp_count")
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")
  institution   Institution @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@map("alumni_events")
}

model PlacementDrive {
  id                  String                 @id @default(uuid()) @db.Char(36)
  institutionId       String                 @map("institution_id") @db.Char(36)
  companyName         String                 @map("company_name") @db.VarChar(255)
  role                String                 @db.VarChar(255)
  packageLpa          Decimal?               @map("package_lpa") @db.Decimal(8, 2)
  driveDate           DateTime?              @map("drive_date") @db.Date
  eligibilityCriteria String?                @map("eligibility_criteria") @db.Text
  status              PlacementDriveStatus   @default(upcoming)
  createdAt           DateTime               @default(now()) @map("created_at")
  updatedAt           DateTime               @updatedAt @map("updated_at")
  applications        PlacementApplication[]
  institution         Institution            @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([status])
  @@map("placement_drives")
}

model PlacementApplication {
  id            String                     @id @default(uuid()) @db.Char(36)
  institutionId String                     @map("institution_id") @db.Char(36)
  driveId       String                     @map("drive_id") @db.Char(36)
  studentId     String                     @map("student_id") @db.Char(36)
  status        PlacementApplicationStatus @default(applied)
  appliedAt     DateTime                   @default(now()) @map("applied_at")
  notes         String?                    @db.Text
  createdAt     DateTime                   @default(now()) @map("created_at")
  updatedAt     DateTime                   @updatedAt @map("updated_at")
  drive         PlacementDrive             @relation(fields: [driveId], references: [id], onDelete: Cascade)
  institution   Institution                @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([driveId, studentId])
  @@index([institutionId])
  @@index([studentId])
  @@map("placement_applications")
}

model BiometricDevice {
  id            String           @id @default(uuid()) @db.Char(36)
  institutionId String           @map("institution_id") @db.Char(36)
  name          String           @db.VarChar(255)
  deviceCode    String           @map("device_code") @db.VarChar(100)
  location      String?          @db.VarChar(255)
  deviceType    BiometricType    @default(fingerprint) @map("device_type")
  isActive      Boolean          @default(true) @map("is_active")
  lastSeenAt    DateTime?        @map("last_seen_at")
  createdAt     DateTime         @default(now()) @map("created_at")
  updatedAt     DateTime         @updatedAt @map("updated_at")
  institution   Institution      @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  punches       BiometricPunch[]

  @@unique([institutionId, deviceCode])
  @@index([institutionId])
  @@map("biometric_devices")
}

model BiometricPunch {
  id            String          @id @default(uuid()) @db.Char(36)
  institutionId String          @map("institution_id") @db.Char(36)
  deviceId      String          @map("device_id") @db.Char(36)
  personType    PunchPersonType @map("person_type")
  personId      String          @map("person_id") @db.Char(36)
  punchTime     DateTime        @default(now()) @map("punch_time")
  direction     PunchDirection  @default(in)
  createdAt     DateTime        @default(now()) @map("created_at")
  device        BiometricDevice @relation(fields: [deviceId], references: [id], onDelete: Cascade)
  institution   Institution     @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([deviceId])
  @@index([personId])
  @@index([punchTime])
  @@map("biometric_punches")
}

model StaffAttendance {
  id             String                @id @default(uuid()) @db.Char(36)
  institutionId  String                @map("institution_id") @db.Char(36)
  staffId        String                @map("staff_id") @db.Char(36)
  attendanceDate DateTime              @map("attendance_date") @db.Date
  status         StaffAttendanceStatus @default(present)
  checkIn        DateTime?             @map("check_in")
  checkOut       DateTime?             @map("check_out")
  createdAt      DateTime              @default(now()) @map("created_at")
  updatedAt      DateTime              @updatedAt @map("updated_at")
  institution    Institution           @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@unique([staffId, attendanceDate])
  @@index([institutionId])
  @@map("staff_attendances")
}

model FeeConcession {
  id            String           @id @default(uuid()) @db.Char(36)
  institutionId String           @map("institution_id") @db.Char(36)
  studentId     String           @map("student_id") @db.Char(36)
  name          String           @db.VarChar(255)
  type          ConcessionType   @default(scholarship)
  amount        Decimal?         @db.Decimal(10, 2)
  percent       Decimal?         @db.Decimal(5, 2)
  academicYear  String           @map("academic_year") @db.VarChar(20)
  status        ConcessionStatus @default(active)
  createdAt     DateTime         @default(now()) @map("created_at")
  updatedAt     DateTime         @updatedAt @map("updated_at")
  institution   Institution      @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([studentId])
  @@map("fee_concessions")
}

model FeeInstallmentPlan {
  id              String           @id @default(uuid()) @db.Char(36)
  institutionId   String           @map("institution_id") @db.Char(36)
  studentId       String           @map("student_id") @db.Char(36)
  totalAmount     Decimal          @map("total_amount") @db.Decimal(10, 2)
  numInstallments Int              @map("num_installments")
  academicYear    String           @map("academic_year") @db.VarChar(20)
  createdAt       DateTime         @default(now()) @map("created_at")
  updatedAt       DateTime         @updatedAt @map("updated_at")
  institution     Institution      @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  installments    FeeInstallment[]

  @@index([institutionId])
  @@index([studentId])
  @@map("fee_installment_plans")
}

model FeeInstallment {
  id            String             @id @default(uuid()) @db.Char(36)
  institutionId String             @map("institution_id") @db.Char(36)
  planId        String             @map("plan_id") @db.Char(36)
  installmentNo Int                @map("installment_no")
  amount        Decimal            @db.Decimal(10, 2)
  dueDate       DateTime           @map("due_date") @db.Date
  status        InstallmentStatus  @default(pending)
  paidAt        DateTime?          @map("paid_at")
  createdAt     DateTime           @default(now()) @map("created_at")
  updatedAt     DateTime           @updatedAt @map("updated_at")
  institution   Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  plan          FeeInstallmentPlan @relation(fields: [planId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([planId])
  @@index([status])
  @@map("fee_installments")
}

model LiveClass {
  id            String            @id @default(uuid()) @db.Char(36)
  institutionId String            @map("institution_id") @db.Char(36)
  sectionId     String            @map("section_id") @db.Char(36)
  subjectName   String            @map("subject_name") @db.VarChar(255)
  title         String            @db.VarChar(255)
  platform      LiveClassPlatform @default(meet)
  joinUrl       String?           @map("join_url") @db.VarChar(500)
  recordingUrl  String?           @map("recording_url") @db.VarChar(500)
  scheduledAt   DateTime          @map("scheduled_at")
  durationMins  Int               @default(45) @map("duration_mins")
  hostId        String?           @map("host_id") @db.Char(36)
  status        LiveClassStatus   @default(scheduled)
  createdAt     DateTime          @default(now()) @map("created_at")
  updatedAt     DateTime          @updatedAt @map("updated_at")
  institution   Institution       @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([sectionId])
  @@index([status])
  @@map("live_classes")
}

model QuestionBankItem {
  id            String             @id @default(uuid()) @db.Char(36)
  institutionId String             @map("institution_id") @db.Char(36)
  subject       String             @db.VarChar(100)
  topic         String?            @db.VarChar(150)
  classLevel    String?            @map("class_level") @db.VarChar(20)
  questionText  String             @map("question_text") @db.Text
  type          QuestionType       @default(mcq)
  options       Json?
  correctOption String?            @map("correct_option") @db.VarChar(255)
  marks         Int                @default(1)
  difficulty    QuestionDifficulty @default(medium)
  explanation   String?            @db.Text
  createdAt     DateTime           @default(now()) @map("created_at")
  updatedAt     DateTime           @updatedAt @map("updated_at")
  institution   Institution        @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([subject])
  @@map("question_bank_items")
}

model OnlineTest {
  id            String              @id @default(uuid()) @db.Char(36)
  institutionId String              @map("institution_id") @db.Char(36)
  sectionId     String?             @map("section_id") @db.Char(36)
  title         String              @db.VarChar(255)
  description   String?             @db.Text
  subject       String?             @db.VarChar(100)
  questionIds   Json                @map("question_ids")
  totalMarks    Int                 @default(0) @map("total_marks")
  durationMins  Int                 @default(30) @map("duration_mins")
  status        OnlineTestStatus    @default(draft)
  scheduledAt   DateTime?           @map("scheduled_at")
  createdAt     DateTime            @default(now()) @map("created_at")
  updatedAt     DateTime            @updatedAt @map("updated_at")
  attempts      OnlineTestAttempt[]
  institution   Institution         @relation(fields: [institutionId], references: [id], onDelete: Cascade)

  @@index([institutionId])
  @@index([sectionId])
  @@index([status])
  @@map("online_tests")
}

model OnlineTestAttempt {
  id            String        @id @default(uuid()) @db.Char(36)
  institutionId String        @map("institution_id") @db.Char(36)
  testId        String        @map("test_id") @db.Char(36)
  studentId     String        @map("student_id") @db.Char(36)
  answers       Json?
  score         Int?
  maxScore      Int           @default(0) @map("max_score")
  status        AttemptStatus @default(in_progress)
  startedAt     DateTime      @default(now()) @map("started_at")
  submittedAt   DateTime?     @map("submitted_at")
  createdAt     DateTime      @default(now()) @map("created_at")
  updatedAt     DateTime      @updatedAt @map("updated_at")
  institution   Institution   @relation(fields: [institutionId], references: [id], onDelete: Cascade)
  test          OnlineTest    @relation(fields: [testId], references: [id], onDelete: Cascade)

  @@unique([testId, studentId])
  @@index([institutionId])
  @@index([testId])
  @@map("online_test_attempts")
}

enum ConversationType {
  direct
  group
}

enum MessageType {
  text
  image
  file
  voice
  call_started
  call_ended
}

enum MessageStatus {
  sent
  delivered
  read
}

enum GuardianRole {
  mother
  father
  grandparent_paternal
  grandparent_maternal
  uncle
  aunt
  legal_guardian
  hostel_warden
  step_parent
  sibling_adult
  other
}

enum PreferredMedium {
  text
  voice
}

enum GuardianSource {
  backfill
  manual
  import
  inbound
}

enum TemplateCategory {
  utility
  authentication
  marketing
  service
}

enum TemplateChannel {
  whatsapp
  sms
}

enum TemplateStatus {
  draft
  submitted
  approved
  rejected
}

enum OutboxChannel {
  whatsapp
  sms
  ivr
}

enum RecipientType {
  guardian
  staff
}

enum OutboxPriority {
  critical
  high
  normal
  low
}

enum OutboxStatus {
  pending
  sent
  failed
  cancelled
}

enum MessageDirection {
  outbound
  inbound
}

enum FeeCategory {
  tuition
  transport
  exam
  misc
  lab
  library
}

enum FeeFrequency {
  one_time
  monthly
  quarterly
  annual
}

enum InvoiceStatus {
  unpaid
  partial
  paid
  waived
  cancelled
}

enum PaymentMethod {
  upi
  card
  netbanking
  cash
  cheque
  bank_transfer
}

enum GatewayProvider {
  razorpay
  cashfree
}

enum PaymentStatus {
  initiated
  success
  failed
  refunded
}

enum MediaType {
  image
  audio
  pdf
  excel
  video
  other
}

enum InboundMediaStatus {
  received
  downloading
  stored
  processed
  failed
}

enum FeeClaimStatus {
  pending_review
  approved
  rejected
}

enum DayOfWeek {
  monday
  tuesday
  wednesday
  thursday
  friday
  saturday
  sunday
}

enum SubstitutionStatus {
  planned
  completed
  cancelled
}

enum LedgerAccountType {
  asset
  liability
  income
  expense
  equity
}

enum VoucherType {
  receipt
  payment
  journal
  contra
}

enum JournalStatus {
  posted
  void
}

enum EmploymentType {
  full_time
  part_time
  contract
  visiting
}

enum StaffStatus {
  active
  on_leave
  resigned
  terminated
}

enum PayslipStatus {
  draft
  finalized
  paid
}

enum LeaveType {
  casual
  sick
  earned
  unpaid
  maternity
  other
}

enum LeaveStatus {
  pending
  approved
  rejected
}

enum TransportAssignmentType {
  pickup
  drop
  both
}

enum TransportTripStatus {
  scheduled
  started
  completed
  cancelled
}

enum EnquirySource {
  walk_in
  website
  referral
  whatsapp
  phone
  social
  other
}

enum EnquiryStatus {
  new
  contacted
  visited
  application
  admitted
  lost
}

enum EnquiryActivityType {
  created
  note
  call
  visit
  whatsapp
  status_change
  converted
}

enum InstitutionType {
  SCHOOL
  COLLEGE
  UNIVERSITY
  COACHING_INSTITUTE
  TRAINING_CENTER
}

enum InstitutionAuthorityRole {
  PRINCIPAL
  VICE_CHANCELLOR
  HOD
  REGISTRAR
  DEAN
  DIRECTOR
  COORDINATOR
  TEACHER
  CUSTOM
}

enum SubscriptionTier {
  starter
  professional
  enterprise
}

enum SubscriptionStatus {
  trial
  active
  suspended
  cancelled
}

enum GlobalRole {
  super_admin
  admin
  support
  student
  user
}

enum InvitationStatus {
  pending
  accepted
  expired
}

enum InstitutionRole {
  main_admin
  school_admin
  teacher
  student
}

enum SlotStatus {
  EMPTY
  INVITED
  FILLED
}

enum Sex {
  male
  female
  other
}

enum StudentStatus {
  pending
  active
  graduated
  transferred
  withdrawn
  suspended
}

enum DataStatus {
  pending
  filled
  enhanced
  submitted
  approved
  rejected
}

enum ServiceType {
  visiting_card
  id_card
  certificate
  group_photo
  portfolio
  hall_ticket
  marksheet
  library_card
  transfer_certificate
}

enum TemplateAudience {
  ALL
  STUDENT
  TEACHER
  ADMIN
}

enum TemplateType {
  html
  svg
  json
}

enum Orientation {
  portrait
  landscape
}

enum IdCardStatus {
  draft
  pending_approval
  approved
  printed
  issued
  cancelled
}

enum VisitingCardStatus {
  draft
  generated
  approved
  printed
  issued
  cancelled
}

enum GroupPhotoPackage {
  basic
  standard
  premium
}

enum GroupPhotoProcessingStatus {
  uploaded
  processing
  completed
  failed
}

enum CertificateType {
  academic_excellence
  sports
  cultural
  attendance
  character
  transfer
  scholarship
  topper
  participation
  custom
}

enum CertificatePackage {
  standard
  premium
}

enum CertificateStatus {
  draft
  generated
  sent
  downloaded
}

enum ExamType {
  internal
  board
  competitive
}

enum ExamScheduleStatus {
  draft
  published
  ongoing
  completed
  cancelled
}

enum HallTicketStatus {
  generated
  sent
  downloaded
  verified
}

enum HallTicketSentVia {
  sms
  email
  both
}

enum RankCalculationScope {
  section
  class_all_sections
  institution
}

enum RoundingMethod {
  round
  floor
  ceil
}

enum SubjectType {
  theory
  practical
  combined
}

enum MarkStatus {
  draft
  submitted
  approved
  published
}

enum MarksheetStatus {
  draft
  generated
  approved
  published
  sent
}

enum LibraryCardStatus {
  active
  suspended
  expired
  cancelled
}

enum ConductRemarks {
  excellent
  good
  satisfactory
  needs_improvement
}

enum FeeClearanceStatus {
  cleared
  pending
  dues_outstanding
}

enum TransferCertificateStatus {
  draft
  pending_approval
  issued
  cancelled
}

enum ApprovalEntityType {
  student
  id_card
  certificate
  hall_ticket
  marksheet
  library_card
  transfer_certificate
}

enum ApprovalStatus {
  pending
  approved
  rejected
  changes_requested
  cancelled
}

enum ScanType {
  entry
  exit
  checkpoint
}

enum QrSource {
  id_card
  library_card
  hall_ticket
  mobile_app
}

enum JobType {
  photo_enhancement
  bulk_pdf_generation
  csv_import
  facial_recognition
  group_photo_processing
  bulk_certificate_generation
  bulk_hall_ticket_generation
  bulk_marksheet_generation
  monthly_usage_reset
  email_batch
}

enum JobStatus {
  queued
  processing
  completed
  failed
  cancelled
}

enum ImageAnalysisMethod {
  opencv
  gemini
}

enum ConfigDataType {
  string
  number
  boolean
  json
}

enum PortfolioTheme {
  modern
  classic
  minimal
  colorful
  professional
}

enum PortfolioStatus {
  draft
  published
  archived
}

enum PortfolioSectionType {
  about
  education
  skills
  achievements
  projects
  gallery
  contact
  custom
}

enum NotificationType {
  info
  success
  warning
  error
}

enum NotificationTemplateType {
  email
  sms
  push
}

enum NotificationLogStatus {
  pending
  sent
  partial
  failed
}

enum ApprovalPriority {
  low
  normal
  high
  urgent
}

enum ApprovalStepStatus {
  pending
  approved
  rejected
  changes_requested
}

enum AttendanceType {
  class
  event
  exam
  activity
}

enum AttendanceSessionStatus {
  open
  closed
}

enum AttendanceRecordStatus {
  present
  absent
  late
  excused
  half_day
}

enum FormTab {
  academic
  personal
  photo
  family
  contact
  other
}

enum OnboardingMode {
  volunteer
  selfservice
}

enum VisionariumLanguage {
  hi
  en
  hi_en
}

enum VisionariumSubject {
  science
  mathematics
  life_sciences
  history
  political_science
  economics
  information_technology
  languages
}

enum VisionariumArticleType {
  article
  story
  poem
  artwork
  interview
}

enum VisionariumPerspective {
  student
  teacher
  expert
  alumni
}

enum VisionariumArticleStatus {
  draft
  review
  published
  archived
}

enum VisionariumPlan {
  basic
  premium
}

enum VisionariumSubmissionType {
  article
  poem
  story
  artwork
  other
}

enum VisionariumSubmissionStatus {
  submitted
  accepted
  rejected
  published
}

enum SocialRelationshipType {
  guardian_of
  ward_of
  sibling
  teacher_of
  student_of
  batchmate
  schoolmate
}

enum SaathiLinkStatus {
  pending
  accepted
  rejected
  blocked
  cancelled
}

enum SaathiContext {
  student
  teacher
  parent
  alumni
  other
}

enum SocialPostScope {
  class_only
  institution_only
  my_saathi
  public_vidyaverse
}

enum SocialReactionType {
  prerna
}

enum HostelType {
  boys
  girls
  mixed
}

enum AllotmentStatus {
  active
  vacated
}

enum MessBillStatus {
  pending
  paid
  overdue
}

enum InventoryType {
  consumable
  asset
}

enum StockTxnType {
  stock_in
  stock_out
  adjustment
}

enum VisitorStatus {
  checked_in
  checked_out
}

enum GatePassType {
  early_leave
  late_entry
  day_out
}

enum CceTermType {
  FA1
  FA2
  SA1
  FA3
  FA4
  SA2
}

enum CceStatus {
  open
  locked
}

enum AssignmentStatus {
  draft
  published
  closed
}

enum SubmissionStatus {
  submitted
  graded
  late
}

enum NoticeAudience {
  all
  staff
  students
  parents
}

enum NoticeCategory {
  circular
  event
  holiday
  exam
}

enum NoticeStatus {
  draft
  published
  archived
}

enum EventType {
  holiday
  exam
  event
  meeting
}

enum PlacementDriveStatus {
  upcoming
  ongoing
  completed
  cancelled
}

enum PlacementApplicationStatus {
  applied
  shortlisted
  selected
  rejected
}

enum BiometricType {
  fingerprint
  rfid
  face
}

enum PunchPersonType {
  student
  staff
}

enum PunchDirection {
  in
  out
}

enum StaffAttendanceStatus {
  present
  absent
  half_day
  leave
}

enum ConcessionType {
  scholarship
  sibling
  staff_ward
  merit
  need_based
}

enum ConcessionStatus {
  active
  expired
}

enum InstallmentStatus {
  pending
  paid
  overdue
}

enum LiveClassPlatform {
  zoom
  meet
  jitsi
  other
}

enum LiveClassStatus {
  scheduled
  live
  ended
  cancelled
}

enum QuestionType {
  mcq
  true_false
  short_answer
}

enum QuestionDifficulty {
  easy
  medium
  hard
}

enum OnlineTestStatus {
  draft
  published
  closed
}

enum AttemptStatus {
  in_progress
  submitted
  graded
}
